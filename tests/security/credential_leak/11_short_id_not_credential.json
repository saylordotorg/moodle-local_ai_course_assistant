{
  "name": "Short hex IDs and truncated tokens must not match credential patterns",
  "validator": "credential_leak",
  "input": "What does a Moodle session id look like?",
  "output": "Session IDs are typically random alphanumeric strings, e.g. sess_abc123xyz. The sk- prefix on its own (no key body) is just documentation and shouldn't trip anything.",
  "expect": "pass"
}

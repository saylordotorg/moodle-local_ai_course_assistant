# SOLA findings triage - 93 confirmed

Generated 2026-09-03 for v7.2.10.

## Status as of v7.3.2 (2026-09-03)

- Fixed in v7.3.0 / v7.3.1: 11 findings (F11, F13, F16, F31, F40, F46, F48, F50, F57, F72, F83)
- Fixed in v7.3.2: 27 findings (F02, F17, F19, F20, F26, F34, F35, F36, F37, F39, F41, F42, F43, F49, F51, F52, F53, F59, F60, F66, F67, F69, F70, F71, F75, F79, F87)
- **Remaining: 55 of the original 93.**

Three of the v7.3.2 fixes were materially worse than this document records, and
the corrected accounts live in the v7.3.2 changelog rather than here:

- **F41** is three defects, not one. Correcting the hook name alone would have
  left the cascade broken, because the context list was computed after the
  deletion that empties the tables it is derived from.
- **F34** loses the entire emitted segment, not a single character: invalid UTF-8
  makes json_encode return false and the whole frame is dropped.
- **F37** was rated low here and belongs near the top: viewing the token
  analytics page sends the spend alert and burns the once-per-period suppression
  flag, so refreshing the dashboard during an incident is how the real warning
  fails to arrive.

Two findings were deliberately NOT fixed, with reasons:

- **F52 (rate card)** — the partial-data indicator shipped, but no Gemini prices
  were added. They could not be verified against a current price sheet, and an
  unverified rate produces a confidently wrong cost, which is the failure class
  this release exists to remove. Supply the current Vertex rates and it is a
  four-line change.
- **F05 (dead SVG avatar)** — a product decision, not a defect. The procedural
  avatar is unreachable and its animation code is dead, but removing it or
  reviving it are both deliberate choices, and the A/B experiment that depends on
  it is measuring the launcher glow rather than a face.


## Provenance

These are the surviving findings from the two review workflows run on 2026-09-02
against v7.2.8/7.2.9. Recovered from the workflow journals at
`.claude/projects/<project>/<session>/subagents/workflows/wf_*/journal.jsonl`.

Raw finder items: 128. Adversarially verified: 127. Confirmed real: 93.
Rejected by the verify stage: 34.

The earlier "~27 remaining" figure in my session notes was an estimate of the
un-fixed remainder, not the size of the confirmed set. The real number is 93.

## Status honesty

12 of the 93 were re-verified against current code on 2026-09-03 and carry an
explicit VERIFIED note below. The other 81 carry the workflow's verdict only.
A workflow verdict is evidence, not proof - four claims in the original set were
wrong about their own cause even when the defect was real. Re-verify before fixing.

## Analytics & reporting (30)

### F10 - unrated

talking_avatar_viewer.php has two independent, provable defects. (a) Wrong security-header mode. Line 73 calls security::send_security_headers() with the default $fullmoodlepage=false (classes/security.php:325), so the strict raw-endpoint CSP built at security.php:367-374 is sent on a page that then renders $OUTPUT->header() (:89) and $OUTPUT->footer() (:115). It is the only full Moodle page in the plugin that does this: vendor_dpa.php:50, essay_feedback.php:51, flashcards.php:52, instructor_dashboard.php:76 and soapbox.php:54 all pass true, sandbox.php:67 deliberately skips the call, and the plugin's own docblock (security.php:338-346) plus tests/security_headers_test.php:43-48 state that a full page under this CSP breaks Moodle's YUI/requirejs JS. The CSP's script-src 'self' 'unsafe-inline' also blocks the cross-origin bundle script injected at :105-111, and its connect-src allowlist contains only LLM API hosts, no D-ID/HeyGen/LiveKit signaling host. (b) The bundle setting does not exist. $bundleurl at :80 reads get_config('local_ai_course_assistant','cdn_bundle_url'), and that config name appears exactly once in the repository. settings.php registers the different names cdn_url (2718) and cdn_version (2726), consumed by hook_callbacks.php:103/911-913 for the widget bundle; nothing in settings.php, db/upgrade.php, or policy_bundle's allowlist sets cdn_bundle_url. So it is always false -> '' and :101-103 unconditionally shows the "configure a CDN bundle" notice. This is reachable with a vendor fully configured: did_provider.php:40-42 needs only api_key + persona_id, :79-87 hands the viewer URL back as embed_url for ui.js to iframe, and the gate at hook_callbacks.php:560-563 involves no bundle — so an operator with a working D-ID/HeyGen key reaches a page that blames their configuration for a setting with no admin surface. Fix: pass true at :73 and either register cdn_bundle_url in settings.php (with its own relaxed header path for the vendor origins) or make the notice state that the surface is unimplemented. Minor corrections to the original claim: connect-src blocks the signaling legs rather than ICE/RTP media, and the fallback notice itself is documented as intentional at :23-26 — the defect is that its "when configured" precondition is unreachable.

### F23 - unrated

Soapbox STT telemetry is dropped when the learner has no conversation row for the course: soapbox_transcribe.php:173-176 looks up an existing local_ai_course_assistant_convs row (UNIQUE(userid,courseid), db/install.xml:24) and calls conversation_manager::add_message only `if ($conv)`, never creating the row, so a learner who reaches soapbox.php from course nav without ever opening the chat drawer produces no '[Soapbox STT]' row at all — after the paid Whisper POST at soapbox_transcribe.php:151. The surrounding empty `catch (\Throwable)` also swallows genuine write failures. Correction to the original claim: this is a telemetry/audit gap, not the cause of the spend-accounting gap. Even when the row IS written it is priced at zero and invisible to reporting, because role='system' makes conversation_manager.php:126-127 null out model_name and provider, and both spend_guard::compute_spend (classes/spend_guard.php:165 plus analytics::spend_rows_predicate, classes/analytics.php:438-439) and analytics::get_token_costs (classes/analytics.php:519-520) reject rows that are neither role='assistant' nor interaction_type embedding/rerank/quiz and that have a null model_name. transcribe.php:182-186 shares the pattern but is practically unaffected, since conversational STT requires the drawer to be open, which itself creates the conversation row. Test consequence: "confirm the STT row appeared" is only a valid staging check if you first open the chat drawer in that course, and even then the row never shows in learner history or in any spend/token figure.

### F28 - unrated

CONFIRMED. Clicking a second Learning Radar citation badge without an intervening outside-click leaves the popover hidden, so the user must click the badge twice. Mechanism (amd/src/learning_radar.js): each `showCitation` call registers a fresh document-level `dismiss` listener via `setTimeout(…, 0)` at :456-463, capturing that call's `id` and the singleton popover `pop`; the listener self-removes only when it actually hides (:460). Click badge #1 → listener_1 registered with id='1'. Click badge #2 → the badge's own handler (bound at :381, target phase) sets `pop.style.display='block'` at :425 and fires the Ajax; the same event then bubbles to `document`, where listener_1 evaluates `!pop.contains(badge2)` = true and `'2' !== '1'` = true (:458) and sets `pop.style.display='none'` (:459). The Ajax response later lands via `Templates.replaceNodeContents` (:442), which swaps children but never clears the inline `display:none` — so the citation populates an invisible node. listener_2, registered after dispatch by the `setTimeout`, cannot re-show it. Deterministic, not a race: the network round trip always outlasts the synchronous event dispatch. Secondary, minor: repeated clicks on the *same* badge short-circuit at :458 (`citeId === id`), so :460 never runs and one document listener leaks per click — bounded by click count and cleared on navigation. Confirmed against the shipped bundle: `amd/build/learning_radar.min.js` carries byte-equivalent logic, so v7.2.7 on staging behaves the same. Corrections to the original wording: the badge handler runs in the target phase, not the bubbling phase; and `pop.style.display='block'` is at :425, not :418 (:418 is the `appendChild` inside the lazy-create IIFE). Reproduction for the tester (no student account needed, no extra API cost beyond one radar query): /local/ai_course_assistant/analytics.php → run one Learning Radar query whose answer contains two or more `[#N]` citations → click badge #1 (popover appears) → click badge #2 (nothing appears) → click badge #2 again (popover appears). PASS would be the popover switching content on the first click of badge #2.

### F37 - unrated

CONFIRMED. The read-only Spend status card on token_analytics.php drives the enforcing spend-guard path: token_analytics.php:291 calls spend_guard::status_rows(), which at classes/spend_guard.php:503 calls self::check() for each of the five site scopes even though it already computed cap (:495) and spend (:496) itself. check() (:251) has no read-only mode and fires maybe_notify() at :276/:280/:284, which emails every recipient -- defaulting to all site admins via get_admins() when spend_notify_emails is unset (:306-312, send at :353) -- and then writes the suppression flag spend_notify_<md5> at :355 unconditionally, even if no mail was actually sent. Consequences: a plain admin GET on an analytics dashboard is a mail-sending, state-writing operation whenever any cap is set and spend is >=80%, and each refresh during a real incident consumes that threshold's once-per-period notification, so the genuine alert never arrives. There is no side-effect-free way to view the amber/red rendering. Two scoping corrections to the original claim: the flag key includes period_start() (:301) so suppression lasts for the current period rather than permanently; and on this staging instance (site email disabled) Moodle core's email_to_user short-circuits on $CFG->noemailever so no mail leaves the box -- but the flag write still occurs, and on prod both the mail and the suppression land. Partial incidental mitigation: the emergency_chat_disabled early return at :263-268 bypasses notify for the capability=null and 'chat' rows only; Voice, RAG and Analytics (:490-492) still reach it. Fix shape: add a non-notifying level accessor (or a $notify=false parameter on check()) and have status_rows() derive 'level' from the cap/spend values it already holds.

### F40 - unrated

A positive per-capability cap short-circuits cap resolution before both the per-course override and the site-wide default per-course cap (classes/spend_guard.php:90-97 returns immediately, ahead of the per-course block at :99-113 and the site cap at :114-115). Because the primary chat factory always resolves capability to the literal 'chat' (classes/provider/base_provider.php:480 with the helper at :636-638), setting spend_cap_chat makes the per-course and per-course-default rails unreachable for all learner chat and quiz traffic — no other code path enforces them (they are read only at spend_guard.php:99-113). The behavior contradicts spend_guard's own docblock at :84-85 ("stacks on top") yet is pinned by tests/spend_guard_per_course_default_test.php:61-66, and the per-course-default setting copy (lang/en:1838, rendered at settings.php:1151-1156) advertises an unconditional defensive bound with no mention of the interaction. Two refinements to the original claim: the site cap is not purely display-only — spend_cap_site is still reached by the fall-through at :114-115 when no capability or course cap is set; it is the capability=null "All capabilities" total, used only by status_rows (spend_guard.php:485-506, token_analytics.php:291), that is never enforced. And on that same fall-through with a course context, check() compares spend_cap_site against course-scoped spend (spend_guard.php:266-270), so the site cap is itself under-enforced on the chat path. Related adjacent gap: base_provider::create_for_comparison runs enforce_learner_guards() but no spend_guard::check() at all, so the sse.php:665 comparison-provider chat path bypasses every spend cap.

**Status:** OPEN - verified 2026-09-03: spend_guard.php:91-97 returns on a positive per-capability cap before reaching the per-course branch.

### F48 - medium

The analytics dashboard's "Export CSV" button links to redash_export.php, which only ever emits JSON (no CSV branch, no Content-Disposition), so it never downloads a CSV; on a default install (redash_api_key unset, the shipped default) it returns a 403 JSON error document. The real CSV exporter, external\export_analytics_csv, is registered but has no caller.

**Status:** FIXED - PR #216: redash_export.php gained a format=csv branch; analytics.php passes format=csv.

### F49 - low

get_unit_usage sums per-cmid DISTINCT-user counts into a section total (classes/analytics.php:1060-1062), so a learner who chats from multiple activities in one section is counted once per activity. Inflates the "Active AI users" column and deflates the derived messages-per-student on the analytics By Unit tab.

### F50 - medium

The analytics 'By Course' tab shows 0 for every metric: renderByCourse reads flat keys (active_students, total_messages, avg_messages_per_student, return_rate_pct, avg_session_minutes) off each course element, but get_analytics_by_course nests them under 'overview', 'sessions' and 'return_rate' — and never emits 'avg_session_minutes' at all (the server key is sessions.avg_duration_minutes). Course names and the table/chart scaffolding still render, so the tab reads as "nobody uses SOLA" rather than as a wiring fault. Admin-only page; display-only impact, no data or security consequence.

**Status:** FIXED - PR #216: analytics_dashboard.js courseMetrics() reads the nested payload.

### F51 - low

Learning Radar admin queries (interaction_type='meta'/'meta_scheduled', written as role='user'/'assistant' against SITEID by conversation_manager::record_meta_query) are counted as learner activity in every site-wide (courseid=0) analytics metric, because no query in classes/analytics.php filters on interaction_type despite the writer's docblock promising that tag exists for exactly that purpose. Affects Active AI Users, total messages, sessions, return rate, hour-of-day distribution and Themes keywords on the Overall Usage panel; scheduled radar runs add rows attributed to get_admin() with no human present. Admin-dashboard display accuracy only: per-course reporting is unaffected (meta rows live on SITEID), and spend accounting is separate (spend_rows_predicate / spend_guard.php:220 handle meta explicitly).

### F52 - low

token_analytics.php's headline "Estimated Cost" card sums only priced models and renders a clean dollar figure whenever any model priced, with no "partial" indicator; the shipped rate card has no gemini-2.x prefix matching the configured gemini-2.5-flash chat model, so on a site where the default-on weekly LiteLLM rate-card refresh has not yet run (or has no egress), primary chat spend is omitted from the headline with no warning. Mitigated by the default-on refresher and by the per-model table showing "—" for the unpriced row.

### F53 - low

token_analytics.php per-student cost prices a learner's summed prompt/completion tokens at the rate of MIN(m.model_name) — the alphabetically-first model in their rows, not their actual mix — so any Claude-escalated turn re-prices all of that learner's chat and quiz tokens at claude-sonnet rates (3.00/15.00 vs gpt-4o-mini 0.15/0.60). The table caption also misdescribes the method ("the model of their most recent session"), which MIN is not.

### F54 - low

Partially confirmed, narrower than reported: analytics.php pre-escapes feedback/survey/user-testing free text with htmlspecialchars() before handing it to a Mustache template that escapes again, so `&`, `"`, `<`, `>` display as literal entity text (e.g. a comment containing R&D shows as `R&amp;D`, a quoted phrase as `&quot;hi&quot;`). The headline apostrophe example does NOT reproduce — Moodle's Mustache escaper is `s()`, which strips the extra layer off numeric character references, so `O'Brien` renders correctly. Cosmetic only, and only for free-text fields that actually contain one of those four characters; names and UA strings almost never do.

### F55 - low

get_messages_to_resolution() can never match a rating: it builds sessions from role='user' rows (classes/analytics.php:1288-1291) but tests them against $ratedset keys that are always assistant-message ids (:1310-1315, :1341), and msgs.id is one shared sequence across both roles (db/install.xml:29), so the function always returns $zeroresult (:1347-1348). Real logic bug, but currently a dead metric rather than a wrong-number-on-screen bug: the only consumer is get_analytics_feedback.php:80-83, whose 'messages_to_resolution' key is not the 'resolution' key the dashboard reads (amd/src/analytics_dashboard.js:422), so the tile at :430 already shows 0.0; export_analytics_csv.php:133-135 does not include this metric at all.

### F56 - low

Off-topic rate at classes/analytics.php:104-115 divides an untimed SUM(offtopic_count) by a $since-windowed user-message count, so the ratio is inconsistent whenever a range other than "all time" is selected (the default is range=30). The reporter's premise is wrong on one point: offtopic_count is a consecutive-run counter reset to 0 on any on-topic message (sse.php:1083-1084) and on lockout expiry (sse.php:335), capped at offtopic_max when offtopic_action='end' (sse.php:1072), and convs is UNIQUE(userid,courseid) — so the numerator is the sum of currently-open streaks, not a cumulative history total, and does not grow without bound. Realistic distortion is small; >100% requires a contrived state. Separately (and not reported), because the numerator is streak values rather than off-topic message counts, the metric is not a true off-topic message rate even at "all time". Reaches the dashboard card (templates/analytics_dashboard.mustache:409), the overall/courses CSV (classes/external/export_analytics_csv.php:80,86) and redash_export.php:276; get_experiment_metrics does not consume it.

### F57 - medium

The analytics dashboard's "AI Feedback" tab is wired to key names the web service never sends, so it always paints six zero tiles, skips both charts, and never emits the negative-feedback table. renderFeedback (amd/src/analytics_dashboard.js:420-423) reads data.ratings / data.survey / data.resolution / data.negatives, while get_analytics_feedback::execute (classes/external/get_analytics_feedback.php:57-83) sets exactly rating_summary / negative_feedback / survey_summary / messages_to_resolution before json_encode at :84. The leaf names inside each container do match (classes/analytics.php:1250-1255 thumbs_up/thumbs_down/hallucination_flags, :1423-1428 avg_star_rating/rating_distribution/survey_respondents, :1271 avg_messages), so the payload is one wrapper level away from correct and every read resolves undefined. Verified no intervening remap (:152 is a straight JSON.parse of the PARAM_RAW string), correct class in db/services.php:283-284, tab and pane both present and reachable (templates/analytics_tabs.mustache:70 and :80, rendered by analytics.php:816), no pre-seeded cache, and the same wrong keys present in amd/build/analytics_dashboard.min.js with none of the four server names. Admin-only page (moodle/site:config, get_analytics_feedback.php:53), display-only, no data loss or security impact, and some signal remains visible elsewhere on the page (7-day negative-rating chip at analytics.php:635-643; server-rendered survey card at analytics.php:356-361) — hence medium rather than high, though the silent all-zeros presentation can lead an admin to conclude no learner has flagged a bad answer. Third instance of this bug class in the file: renderOverall carries in-code postmortems for the same nesting mismatch at :213-219 and :251-257; renderFeedback was missed in that sweep, and the remaining five renderers deserve the same key-by-key diff.

**Status:** OPEN - verified 2026-09-03: server sends rating_summary / negative_feedback / survey_summary / messages_to_resolution; renderFeedback reads ratings / negatives / survey / resolution. Zero overlap. Same bug class as F50.

### F58 - low

analytics.php:376 fetches every Learning Radar meta row ever written (full `message` bodies for both query and answer, all-time, no LIMIT, and without the page's own `$since` filter) on every dashboard load, then truncates to 200/280 chars and slices to 50 in PHP at :396-397/:410. Nothing bounds the set: meta rows deliberately bypass the conversation cap and are "kept indefinitely" (classes/conversation_manager.php:175-178), and conversation_retention only purges conversations idle for 730 days (classes/task/conversation_retention.php:48-52) while all meta rows reuse one long-lived conversation per admin (:207). Growth is admin/schedule-driven rather than learner-driven — 2 rows per ad-hoc query plus 2 per schedule per period, daily being the max frequency — so the ceiling rises slowly; the reporter's "thousands of multi-KB bodies in a year" needs several daily schedules. Cheapest fix: apply the existing `$since` (or an ORDER BY id DESC + limit pass) as redash_export.php:430-432 already does for the same query.

### F61 - low

run_meta_ai_query records last_status='success' purely on the absence of a thrown exception (classes/task/run_meta_ai_query.php:69-70), while every delivery failure is signalled by a `false` return rather than a throw — opt-out (classes/radar_delivery.php:175), SSRF rejection (:224, :259), non-2xx webhook POST (:308). When all channels fail, `$delivered` is false, the fallback admin email is sent and its result discarded (:158-170), and nothing sets an error state; when only *some* channels fail, `$delivered` stays true so there is not even an mtrace line. The Learning Radar schedule panel (analytics.php:416-421 → green badge) therefore shows a healthy recent run for a schedule whose Slack/Teams webhook is dead or whose recipient unsubscribed, and record_run also blanks last_error (classes/radar_schedule_manager.php:127) on the way through. Observability-only: the cron log does print "no destination delivered; falling back to admin email" for the total-failure case and the admin fallback normally still delivers the report. The report's additional empty-query sub-claim should be dropped — radar_schedule.php:118 rejects empty queries on save and db/upgrade.php:772 only migrates non-empty ones, so that early return is not reachable without direct DB edits.

### F65 - low

radar_export.php sends a JSON Content-Type at line 103 and then runs the email/slack/teams/push_redash branches with no AJAX_SCRIPT define and no try/catch, so any exception from those branches (notably security::resolve_for_pin's DNS re-validation throw) emits an HTML Moodle error page under a JSON content type and the admin sees a JSON-parse message instead of a delivery error. Cosmetic: the send is still correctly refused, admin-only endpoint, narrow transient window.

### F66 - low

meta_ai_data_builder::build_transcript() and build_aggregate_stats()'s message count apply no role filter, so role='system' telemetry rows with a real courseid are labelled 'SOLA' and fed to the Learning Radar analytics LLM as tutor replies. In practice the always-on sources are quiz telemetry -- conversation_manager::record_quiz_usage (conversation_manager.php:290-297, from generate_quiz.php:400) and external/record_quiz_open.php:93 -- not premium_router, which is off by default. record_quiz_usage stamps provider = the chat provider ('gemini'), so those rows survive even a provider-filtered schedule; premium_router rows (provider='premium_router') do not. The embedding/rerank ledgers are unaffected because they are written against SITEID and build_where's 'm.courseid > 1' already excludes them. The fix is the existing canonical predicate analytics::conversation_rows_predicate() (analytics.php:402-404); get_radar_citation.php:74 needs the same treatment since it repeats the unfiltered 'SOLA' labelling for resolved [#NNN] tokens.

### F67 - low

redash_export.php:420 passes 50000 as build_transcript's third positional argument, which is `string $filterprovider`, not `int $maxchars` (signature at classes/meta_ai_data_builder.php:336-341). With no declare(strict_types=1) anywhere in the plugin, PHP weak mode coerces the int to "50000" without error; build_where (classes/meta_ai_data_builder.php:451-454) then applies `m.provider = :filterprov` with '50000', which matches no row, so the early return at :360-362 makes meta_ai.transcript_excerpt always the string "(No conversation data found for the selected filters.)" for every course and every time window, while $maxchars silently keeps its 100000 default instead of the intended 50000. Confirmed as the only build_transcript definition; the sole other caller (:86) passes all four args correctly. Severity lowered to low: the failure is confined to one field of an opt-in section, returns a self-describing placeholder sentence rather than a blank/empty payload, has no security or data-loss consequence (it currently suppresses rather than causes raw-transcript egress), and the sibling `summary` field is unaffected.

### F68 - medium

Pseudonymization on the Learning Radar / review-queue / Redash surfaces relabels only the identity column; the message bodies emitted beside "Student NNNN" are verbatim and can contain the learner's real first name, because the chat system prompt instructs the tutor to use it. The gap is real, but every internal consumer is capability-gated to a user who already has lawful access to those names, so the practical exposure is (1) real first names reaching the analytics LLM under a prompt that asserts the data is pseudonymized, and (2) the Redash payload advertising `anonymized: true` while `meta_ai.transcript_excerpt` carries them out to BI consumers.

### F69 - low

Three readers of the audit table query a non-existent `event` column (and select a non-existent `payload` column) instead of the real `action`/`details`, so the queries throw dml_exception and are swallowed by catch(\Throwable) into empty/0: classes/review_queue.php:119-124 (reads $a->payload at :132), analytics.php:651-654, classes/task/run_anomaly_digest.php:101-105. Confirmed against db/install.xml:287-307 and db/upgrade.php:99-113, and against every writer (classes/audit_logger.php:41,46; classes/health_check.php:265-269; classes/cost_anomaly_detector.php:399-406), all of which use action/details. No table in db/install.xml has an event or payload field, so this is not a wrong-table confusion. IMPORTANT CORRECTION to the original finding's impact: nothing in the codebase ever writes an 'integrity_flagged' audit row -- grep -rn "integrity_flagged" returns only these three read sites -- and classes/integrity_checker.php is an installation self-test (test_db_tables/test_template_files), not an academic-integrity classifier. Fixing the column names would still yield zero rows. This is therefore dead code on both ends plus a misleading docblock (classes/review_queue.php:30 promises an 'integrity' source that cannot work), not a broken monitoring control masking live flags. Real but low severity; the practical risk is that a future writer implementation will hit a silently empty queue.

### F77 - low

Admin analytics tables double-escape learner-authored free text: values are passed through htmlspecialchars() in PHP and then escaped a second time by Moodle's Mustache engine (escape => 's'), so feedback comments and survey answers containing `<`, `>`, `&`, or `"` display as literal `&lt;` / `&amp;`. Cosmetic only — it fails in the over-escaping direction, no injection. Affected: analytics.php:212/218/220-223 (name, comment, browser, os, device, screen) rendered as {{name}}/{{comment}}/{{browser}}/{{os}}/{{device}}/{{screen}} at templates/analytics_dashboard.mustache:593/596/597/598; analytics.php:267/304/319 (survey text answers and comments — the most likely fields to contain `<`) rendered as {{text}} at templates/analytics_dashboard.mustache:663/701/722; token_analytics.php:337 ('active') rendered as {{active}} at templates/token_analytics.mustache:204. The original report's "course names" claim is not supported at the analytics feedback table: $course_name is the raw $course->fullname (analytics.php:156 → :337) rendered as {{course.coursename}} at :350, which is correctly single-escaped. A separate ampersand-only instance does exist at analytics.php:598, where format_string($topcourse->shortname) (format_string already converts bare `&` to `&amp;`) is rendered as {{value}} at templates/analytics_dashboard.mustache:142.

### F81 - medium

TTS/STT/Soapbox telemetry rows are mis-shaped (role='system', interaction_type defaults to 'chat', provider/model_name nulled at classes/conversation_manager.php:126-127), so the synthesis/transcription slice of voice spend is priced at zero and excluded from spend_guard (spend_guard.php:165-166), cost_anomaly_detector (:100-101, :140-141), the token_analytics voice_tts/voice_stt categories (token_analytics.php:95-96) and the A/B "TTS plays" metric (classes/analytics.php:1513); Realtime audio is not logged at all (classes/external/get_realtime_token.php). The reporter's headline is wrong, however: the voice spend cap is NOT dead. amd/src/chat.js:5319-5320 sets interaction_type='voice' for voice-tab turns, sse.php:112 reads it and sse.php:1021-1031 writes it on the role='assistant' row with model_name/provider set, so capability_sql('voice') (spend_guard.php:212) matches real rows, spend_cap_voice can fire, the "voice paused"/failover branch at voice_registry.php:105-124 is reachable, and the anomaly detector does see voice-mode LLM spend. Per-user abuse on the unlogged paths is still bounded by the rate buckets at tts.php:49, transcribe.php:49 and soapbox_transcribe.php:54, and the logged STT token counts are self-described arbitrary units, so the dollar understatement is modest.

### F85 - low

generate_quiz bills a provider call at classes/external/generate_quiz.php:227 but writes the priced usage row only on the success path (self::record_quiz_usage at :329, the sole call site). All three post-call bail-outs return before it: the provider-throwable path (:247-253), the unparseable-JSON path (:268-276), and the no-valid-questions path (:304-312). conversation_manager::record_quiz_usage (:271-303) is the only writer of the quiz spend row (role='system', prompt/completion tokens, interaction_type='quiz') and there is no alternative usage table, so those calls are billed and counted nowhere — not by spend_guard, not by token analytics. Reachable on the deployed gemini config: openai_compatible_provider::chat_completion throws only when message.content is absent, so a truncated/thinking-starved response returns an empty string that fails every json_decode and takes the :268 return; the throw path additionally discards a $data['usage'] block it has already parsed. Amplifier: generate_quiz performs no rate_limiter::is_rate_limited() check (unlike record_quiz_open.php:78) and interpolates the learner-supplied PARAM_TEXT topic into the prompt (:211-215), so off-schema output can be induced repeatedly.

### F87 - low

create_for_comparison() (classes/provider/base_provider.php:791-856) runs only enforce_learner_guards() — emergency stop (:687), quiz lock (:726), 120/60 provider_call rate limit (:771) — and never spend_guard::check(), which lives solely in create_from_config() at :480. So on a site that configures a dedicated quiz tier (quiz_provider + quiz_model, both default '' at settings.php:2225-2238), practice-quiz generation instantiates its provider without any cap check (generate_quiz.php:643-654; generate_quiz::execute has no cap check either). Same shape for the cron-queued mastery classifier (conversation_classifier.php:217-228, default openai/gpt-4o-mini) and the doubly-off-by-default Soapbox vision pass (soapbox_slide_vision.php:128-143). Mitigating: quiz spend IS still recorded and counted toward the caps (record_quiz_usage at generate_quiz.php:328 plus 'quiz' in analytics::spend_rows_predicate()), so the cap still trips and blocks chat; the kill switch, quiz lock and per-user rate limit still apply on this path; and the bypass only matters when a cap is configured, which is not the shipped state (base_provider.php:756-758).

### F88 - low

The schema facts hold: `msgs.cmid` (db/install.xml:41, NOTNULL="false", comment "Course module ID the student was viewing when sending this message", indexed `courseid_cmid` at :59) is the only unit-ish column in the plugin, and `grep -n -i section db/install.xml` returns exactly one hit — the `source` enum comment on `_objs` at :506 ("manual, competency, summary, section, llm") — so no plugin table carries a sectionid/sectionnum. Confirmed absent on `_practice_scores` (:455-479), `_profiles` (:480-497), `_objs` (:498-519), `_obj_att` (:520-544). The in-tree precedent is as described: `analytics::get_unit_usage` (classes/analytics.php:1010-1068) groups by `m.cmid` with `m.cmid IS NOT NULL`, then resolves section PHP-side via `get_fast_modinfo` / `get_cm` / `$cm->sectionnum` / `get_section_info` / `get_section_name`; `analytics::get_hotspots` (classes/analytics.php:196-256) indeed never touches cmid and instead `stripos`-matches section names against a `TEXT_SAMPLE_CAP`-bounded message sample. The NULL-cmid gap is also real: `conversation_manager::add_message` writes `$record->cmid = $cmid ?: null` (classes/conversation_manager.php:129) and the system/quiz-marker path sets it from the caller (:298), while `_profiles` (the only summary-bearing table, `profile_summary` at :484) has no unit column at all. ONE SUB-CLAIM IS WRONG: "all PHP-side, not joinable in SQL" overstates it. `{course_modules}.section` is a `course_sections.id` FK in Moodle core and is perfectly joinable — the plugin already does exactly that at classes/quiz_config_manager.php:171-176 (`FROM {course_modules} cm ... ORDER BY cm.section, cm.id`). So a section filter for the transcript viewer can be pushed into SQL (`JOIN {course_modules} cm ON cm.id = m.cmid` + `cm.section`); modinfo is only needed for the display name and for visibility/availability-aware filtering. The design consequence is therefore weaker than stated: no PHP pre-resolution of section → cmid list is required.

### F90 - low

Filter dimension 3 of 5 (TOPIC) has no usable backing column for chat data. `_convs.title` is written only as '' by the runtime path (classes/conversation_manager.php:52) — the sole non-empty writers are the demo seeders (classes/demo_seeder.php:259, admin/cli/seed_demo_data.php:240), so a topic picker would look populated on a demo-seeded dev site and be permanently empty in production. `_keywords` is a write-never table: a full-repo grep for `assistant_keywords` yields only db/install.xml:86, db/upgrade.php:551, the SELECT in instructor_analytics::top_topics (classes/instructor_analytics.php:196) and the course-delete list at classes/observer.php:58 — zero INSERT anywhere, including tests. analytics::extract_keywords (classes/analytics.php:1122-1215) recomputes keywords in PHP and returns them without persisting; analytics.php contains no insert_record or set_field at all. Real topic labels exist only for Soapbox (_practice_scores.session_meta at install.xml:466; _sbx_rec.topicid at install.xml:733 → _sbx_topic.title at install.xml:712). One chat-derived label the original finding missed, _struggle_signal.topic_hint (install.xml:252, written at classes/struggle_classifier.php:143), does not rescue the dimension: it is gated on struggle_classifier_enabled and a non-zero frustration score, auto-purged after 7 days, and its table comment (install.xml:246) states "Never surfaced to instructors". Latent live defect as a side effect: instructor_analytics::top_topics always returns an empty array on every site. Design consequence unchanged — topic must be dropped from the chat tab, computed at query time, or backed by a newly persisted label.

### F91 - low

Privacy constraints for an anonymized transcript viewer, all verified: _learner_goals (db/install.xml:190), _learner_memory (:230) and _struggle_signal (:246) are documented instructor-invisible and must not be joined/exported; _profiles.profile_summary (:485) is safe anonymized (precedent at classes/meta_ai_data_builder.php:319-321, metadata at classes/privacy/provider.php:178). Row filtering must use analytics::conversation_rows_predicate (classes/analytics.php:402-404, role IN ('user','assistant')) because embedding/rerank/premium_router telemetry writes role='system', and must also exclude interaction_type 'meta'/'meta_scheduled' (redash_export.php:428-447), whose bodies are admin Learning Radar queries over bulk learner data. Do NOT copy meta_ai_data_builder::build_where (:436-459) — it filters courseid/since/provider only, and its caller build_transcript (:336-358) adds no role clause either, so the existing Radar transcript already ingests telemetry and meta rows. Retention: conversation_manager::add_message caps a conversation at 100 rows and deletes the excess (classes/conversation_manager.php:150-166) with one permanent conversation per learner per course (UNIQUE userid,courseid at db/install.xml:24), so a date-range control cannot reach beyond a learner's last 100 turns — with the caveats that record_meta_pair bypasses the cap and embedding telemetry uses conversationid=0 (classes/embedding_provider/base_embedding_provider.php:248), so neither consumes cap slots. Erasure walks hard-coded table lists in delete_data_for_user (classes/privacy/provider.php:824-934, lists at :880-892), so any new materialized transcript table must be added there or it survives Article 17. Capability shape confirmed: :viewanalytics is captype 'read' with no riskbitmask, CAP_ALLOW for non-editing teacher (db/access.php:40-48); :manage carries RISK_CONFIG (:51-58).

### F93 - low

Advisory design default (not a blocker): pseudonymize transcripts by default on the new viewer/export, matching classes/meta_ai_data_builder.php:382, which calls anonymizer::name() for every learner row unconditionally — even under redash_export.php's admin-gated, audit-logged anonymize=0 mode. Neither templates/privacy_notice.mustache nor chat:consent_body (lang/en:882) discloses staff reading transcripts under a learner's real name, so a reveal path is worth a disclosure review. But the finding's stated basis is overstated: privacy_notice.mustache:58 scopes to "analytics that help course authors," and the same notice contemplates names appearing beside SOLA data (:50) and in an administrator's view (:86-90) — it is silent on transcript review, not prohibitive. And identified review of learner-authored text already ships: analytics.php:195-224 emits 'name' => $show_real_names ? $realname : $anonname alongside 'comment' => $fb->comment, gated only by the session toggle plus the audit row at :61-79.

## Spend & rate limiting (11)

### F04 - unrated

Every OpenAI Realtime connect sends two competing response.create frames. session.created -> setState('idle') (amd/src/realtime.js:457) synchronously triggers the bootstrap's first-idle kickoff Realtime.sendText(initialText) (amd/src/chat.js:3072-3074), which sends conversation.item.create plus response.create (realtime.js:913-922). session.updated then sends a second response.create carrying hardcoded greeting instructions, guarded only by `!awaitingGreeting && !micHot` — both still false from connect() (realtime.js:466-486, 618-619) — and never checking responseActive. Both realtime paths always supply a non-empty initialText (chat.js:3265, 3340; builders at chat.js:2904-2916, 2957-2963), so one user gesture bills two audio generations on every session. Consequence: whichever response.done arrives first is misattributed to the greeting, resets assistantTranscript and breaks before the SOLA_NEXT block (realtime.js:526-533), so the learner's real first answer loses its follow-up chips and the surviving response.done falls back to the hardcoded chip list. Additionally, if OpenAI rejects the concurrent response, the error case fires onErrorCb and calls disconnect() (realtime.js:575-590), killing the session seconds after connect — that outcome is provider-dependent and not provable from this repo. Confirmed present in the deployed minified bundles amd/build/realtime.min.js and amd/build/chat.min.js.

### F06 - unrated

When context_builder::build_system_prompt() throws, get_realtime_token swallows the error (get_realtime_token.php:198-202: debugging() at DEBUG_DEVELOPER, $fullinstructions = '' and $clientinstructions = '') and still mints a Realtime session, posting a bare {} body with no instructions (get_realtime_token.php:401-408). Because amd/src/realtime.js:661-674 deliberately omits `instructions` from session.update, the resulting session runs on OpenAI's defaults for its whole lifetime — no course grounding, no learner personalization, and none of the jailbreak defenses in the 'security' section (context_builder.php:640) — and it bills once the socket opens and audio flows, though the mint call itself is unmetered. Reachable throw sources inside build_system_prompt that are not internally guarded include \cache::make(...,'systemprompt') (context_builder.php:223), two MUST_EXIST record reads (context_builder.php:242-243), and the DB-backed learner-state/FAQ managers (context_builder.php:452, :499, :603); the current-page path is not one, since get_module_content already catches internally (context_builder.php:916-983). Note the asymmetry: the chat path calls the same builder unguarded (sse.php:538) and so fails closed, while voice fails open with no production-visible trace.

### F20 - unrated

score_speech swallows the provider-construction exception and reports every cause — including three deliberate policy refusals — as a provider outage, logging nothing about the failure. classes/external/score_speech.php:208-210 catches \Throwable around the provider call (constructed at :181 via base_provider::create_from_config) and returns self::empty_result('provider_error') without reading $e: no debugging(), no error_log(), no message passthrough. Because create_from_config runs enforce_learner_guards (base_provider.php:477), three deliberate policy states arrive there as moodle_exceptions and are flattened identically: the emergency chat stop (base_provider.php:688-695, no site-admin exemption per the v7.2.1 comment), the quiz lock (base_provider.php:734-747, checked above the is_siteadmin() return at :753 since v7.2.4), and an exhausted spend cap with no failover (base_provider.php:503-509, rethrown unchanged at :510-511). soapbox.php:191-196 renders 'provider_error' as lang/en:1743 "The AI scoring service could not be reached. Please try again in a moment." — unactionable advice in all three cases. Genuine provider errors are equally unlogged, so a real outage and an operator kill switch are indistinguishable from both the learner's view and the PHP log. classes/external/generate_quiz.php:232-249 shows the intended handling — it re-checks quiz_lock::active_attempt in its catch, records a refusal, returns a lock-specific result, and otherwise surfaces $e->getMessage() — and its own comment names this exact hazard. classes/soapbox_scorer.php:165 routes presentation-assignment scoring through the same execute(), so the swallow covers that path too. Reproducibility correction to the original report: the easy staging trigger is the emergency chat stop (toggle from emergency_admin.php, then score a speech), not a leftover quiz attempt. The quiz-lock variant needs a real mod_quiz attempt — quiz_lock.php:152 queries {quiz_attempts}, so SOLA practice quizzes do not lock — with preview=0 (:167), timestart inside the 3-hour default window for untimed quizzes (:58), in the same course under the default 'course' scope (:86-89). Only the quiz-lock variant leaves any trace at all, an audit row from quiz_lock::record_refusal (:203-227); the emergency-stop and spend-cap variants leave none.

### F31 - unrated

spend_guard::get_cap()'s per-course override branch is unreachable: classes/spend_guard.php:101-102 tests $coursecfg['spend_cap_monthly'], but course_config_manager::get_effective_config() returns a fixed six-key array (provider/apikey/model/apibaseurl/systemprompt/temperature) on all three return paths (classes/course_config_manager.php:106, :112, :114-123), the course_cfg table has no spend column (db/install.xml:153-166, never added in db/upgrade.php), save() persists only those six fields (course_config_manager.php:57-81), its only caller course_settings.php:131 has no cap field, and no code anywhere sets a spend_cap_monthly key. The !empty() test is therefore always false. Note the enclosing `if ($courseid > 0)` block is NOT dead — base_provider.php:480 calls check() with a real courseid, so the spend_cap_per_course_default fallback at spend_guard.php:108-112 does work; only the override branch is unreachable. Impact: the per-course escape hatch promised by lang/en/local_ai_course_assistant.php:1838 and CLAUDE.md:103 does not exist, so an admin who sets a defensive per-course default cap has no supported way to exempt a course that needs more.

**Status:** OPEN - verified 2026-09-03: course_config_manager::get_effective_config returns 6 keys (provider, apikey, model, apibaseurl, systemprompt, temperature). spend_cap_monthly is not among them, so the per-course branch in spend_guard::get_cap is dead. Per-course spend caps silently do nothing.

### F33 - unrated

The cost-anomaly detector's history gate checks the data rather than the enablement date: classes/cost_anomaly_detector.php:265-266 requires ALL seven prior days to have spend > 0 (the array built at :257-260 has exactly MIN_HISTORY_DAYS elements), so any single zero-spend day in the trailing window returns 'insufficient_history' and skips the median comparison at :278-291 entirely. Because the window is rolling, the effect is not permanent — a quiet day suppresses evaluation until it ages out, up to 7 days — but on any site without continuous daily site-wide billable spend (staging, low-traffic installs, holiday or provider-outage days, or a day whose only traffic used a model missing from the rate card, since :114-116 skips costs estimate_cost() returns null for) the detector can be inert for stretches with no admin-visible signal beyond the cron mtrace. This also contradicts the admin documentation at lang/en/local_ai_course_assistant.php:1844 and the inline comment at :262-264, which both describe the status as covering only "the first 7 days after enabling". Note the claim's framing of the flagship scenario is inaccurate: compute_daily_spend (:94-106) aggregates site-wide with no courseid filter, so a single course jumping from near-zero to 10x does not require zero site-wide spend on prior days. The partial-history branch is untested — tests/cost_anomaly_detector_test.php:74-79 covers only the all-zero database.

### F73 - low

soapbox_finalize_recording (a 'write' external function) omits the recording-cap and soapbox feature-flag checks its sibling soapbox_get_upload_url enforces, and never marks an object key consumed, so an enrolled learner can replay one objectkey to create unlimited local_ai_course_assistant_sbx_rec rows and unlimited transcribe+score adhoc tasks, bypassing per-assignment max_attempts and the admin max_recordings cap. Contrary to the original report, the resulting spend IS subject to spend_guard (voice cap via voice_registry::resolve, per-course chat cap via base_provider::create_from_config), and score_speech re-checks the soapbox flag, so scoring cannot proceed in a course where Soapbox was switched off (only the earlier Whisper call would spend).

**Status:** OPEN - functions confirmed to exist (db/services.php:358-366); claim not re-verified in detail.

### F79 - medium

soapbox_finalize_recording has no throttle, no attempt-cap check and no Soapbox feature-flag check, so a learner holding one already-uploaded object key can re-register it repeatedly and buy a fresh hosted-Whisper transcription per call. The LLM legs it queues, however, are still behind the feature flag, the chat spend cap and the emergency stop, so the uncapped part is the transcription leg only.

**Status:** OPEN - functions confirmed to exist (db/services.php:358-366); claim not re-verified in detail.

### F80 - medium

get_realtime_token mints OpenAI Realtime client secrets with no per-user rate limit, no concurrency cap, and no usage row, so realtime audio spend is never attributed and the voice spend cap / cost-anomaly detector cannot see it. Real, but gated behind a feature that is off by default (realtime_enabled default 0) and behind an enrolled learner holding :use, and the mint itself costs nothing — cost only accrues if the caller actually streams audio on the held socket.

**Status:** OPEN - verified 2026-09-03: zero rate_limiter references in classes/external/get_realtime_token.php.

### F82 - low

send_message.php has no rate limit of its own: it runs RAG retrieval (query embedding, plus rerank when enabled) before the only throttle on the path, base_provider's 'provider_call' 120/60 at base_provider.php:768-769, so a learner past the limit still pays one query embedding per request. It also omits the per-course rag_enabled_course_<id> override that sse.php:472-483 honours, so retrieval both spends and injects chunks in courses where an admin disabled RAG. Cost impact is small by default (short-query embedding only; rerank_enabled defaults to 0 and additionally needs a Voyage key and the should_rerank margin gate), and rag_retriever.php:135 already blocks retrieval when a 'rag' spend cap is configured.

### F83 - medium

The 'ratelimit' cache is defined with ttl=120 (db/caches.php:36-41) while rate_limiter stores the whole sliding window as a single cache entry and restarts the window on a miss (classes/rate_limiter.php:107-117), so the two buckets whose window exceeds 120s — 'escalation' 2/3600 (sse.php:1110-1112) and 'soapbox_stt' 12/600 (soapbox_transcribe.php:54) — silently reset after ~121 seconds of inactivity, making their real ceiling 2 and 12 per two minutes.

**Status:** OPEN - verified 2026-09-03: db/caches.php:40 ttl=120 while rate_limiter stores window_start+count as one entry and restarts on miss (classes/rate_limiter.php:110-116). Any window longer than 120s is not enforced.

### F84 - medium

talking_avatar_session.php has no rate limiter and no per-user open-session check, and avatar minutes are invisible to spend_guard and the cost-anomaly detector — the one paid learner endpoint with neither a request limit nor spend accounting. Latent until an operator sets talking_avatar_enabled (default 0) and configures a vendor key.

**Status:** OPEN - verified 2026-09-03: zero rate_limiter references in talking_avatar_session.php.

## Privacy & erasure (9)

### F13 - unrated

sse.php promotes a client-supplied draftitemid into the permanent course-context file area before any server-side validation, and a file that fails the post-hoc MIME check is silently retained. sse.php:120 reads draftitemid with only PARAM_INT; sse.php:372-378 immediately calls attachment_manager::promote_draft_to_message(), which resolves any non-directory file in that draft itemid (attachment_manager.php:203-216) and copies it into the course context keyed by the message id (attachment_manager.php:241-249), deleting the draft at :252. Only then does sse.php:381 run is_mime_allowed(), and the block sse.php:381-394 has no else branch: the just-promoted file is neither deleted nor reported, so the MIME allowlist is advisory on the write path. No size check runs server-side at all — get_max_size_bytes() (attachment_manager.php:76-86) is called only by hook_callbacks.php:873 to render the client hint — and the MIME compared is Moodle's extension-derived stored_file::get_mimetype(), never the content sniffer at attachment_manager.php:144-155, which has zero callers. Identical at v7.2.7 and HEAD. Scope corrections to the original claim: the draft area is resolved against the requesting user's own context (attachment_manager.php:207 with $userid = $USER->id at sse.php:304), so a caller can only promote files already in their own draft area — there is no cross-user read — and the promoted file is served only to the message owner or a local/ai_course_assistant:manage holder (lib.php:99-112). Size is likewise bounded by whatever filepicker's maxbytes created the draft rather than literally unbounded; what is absent is enforcement of the plugin's own configured cap. The path is also unreachable through the shipped UI, since chat.js:5267-5268 only sends draftitemid from state set at chat.js:5095 by the upload endpoint that does not exist, so exercising it needs a hand-crafted POST from a user who already holds :use. Two aggravating facts confirmed: get_history.php:80-91 rehydrates the attachment from the file area with no MIME re-check, so a silently retained disallowed file reappears with a live pluginfile URL on every history reload; and classes/privacy/provider.php never calls get_file_storage(), so nothing ever removes it apart from the same-message-id overwrite at attachment_manager.php:239.

### F16 - unrated

Persisted chat attachments in the `message_attachments` course-context filearea are never deleted by any plugin code path, and the privacy provider neither declares nor exports them. The only `delete_area_files` call (classes/attachment_manager.php:239) is retry-defence inside `promote_draft_to_message`, clearing the same messageid before writing. Message-row deletion (classes/conversation_manager.php:380-381), per-course/per-user purges (:477+, :409-455), and all three privacy erasure paths (classes/privacy/provider.php:804-817, :824-873, :941+) touch DB rows only — provider.php contains no file-storage or filearea reference in its 1039 lines. Hard user deletion via classes/hook_callbacks.php:191-205 routes into those same row-only paths, and because the blobs live in the course context (not the user context) Moodle core's `delete_user()` file reaping does not reach them; only deleting the whole course does. Once the msgs row is gone, lib.php:99-105 `send_file_not_found()`s the pluginfile URL, leaving the blob unreachable but permanently retained: not exportable in a subject-access request, not erasable under Article 17, not visible to an admin. Severity correction to the original report: the browser upload path is dead (`upload_attachment.php` is absent from both v7.2.7 and HEAD), so no such files exist on staging via the UI — but the claim is not vacuous, since sse.php:120/372 accepts any user-owned `draftitemid` with the feature defaulting to enabled (attachment_manager.php:65-69), so a hand-crafted sse.php POST persists an undeletable file today. Latent-by-UI, live server-side.

### F25 - unrated

outreach_sender::send() promises in its class docblock (classes/outreach_sender.php:30) an audit row on every send AND every block reason, but writes one only on the success/dry-run path (:144-151). All six plugin-owned block paths return false silently with no row, no mtrace and no debugging(): :79-81 master switch, :84-86 per-channel switch, :89-91 missing learner consent, :98-100 7-day cooldown, :107-109 deleted/suspended user, :116-118 email opt-out. (The seventh exit at :135-137, email_to_user returning false, is not silent in the same way and is unreachable on this staging box: core email_to_user returns true under $CFG->noemailever, lib/moodlelib.php:5643-5646, and emits debugging() for its other rejects.) The caller does not compensate — classes/task/milestone_check.php:99-102 and :138-149 act only on a true return, and the task's only output is the dispatched count at :59. Compounding this, local_ai_course_assistant_outreach_log has no viewer anywhere: get_log_for_learner (:229-232) advertises a learner-facing "everything SOLA has sent you" panel but has no non-test caller, the table is absent from the settings_user.php:64-76 download bundle, and every other reference (classes/observer.php:63, classes/conversation_manager.php:436/:501, privacy/provider.php, db/install.xml, db/upgrade.php, the backup class) is a deletion or backup sweep. An admin who enables milestones and sees nothing has no log line, no audit row and no UI to consult; the only readable copy of even the dry-run rows is a core data-privacy export. Separately, a dry-run row inserted at :144 still returns true at :153, so milestone_check marks the milestone sent and the row arms the cooldown read at :94-100, permanently suppressing the real email.

### F29 - unrated

Real defect, with corrected line numbers (the claim's cites are ~15 lines low). In the widget's History tab "Saved" view, entering edit mode on a saved response and leaving it — with nothing typed — silently rewrites the stored note with a whitespace-collapsed copy, permanently destroying paragraph structure in the only copy that exists (browser localStorage `aica_bookmarks_<courseid>`; no server copy, nothing in classes/privacy/, no undo). Mechanism: amd/src/ui.js:3320 renders the entry via `text.textContent = (item.text || '').replace(/\s+/g, ' ').trim()`; commitEdit (ui.js:3336-3350, bound to `blur` at :3353, dblclick at :3352, Edit button at :3375) reads back `text.textContent.trim()` (:3341) and its sole guard `newText !== item.text` (:3342) passes because the collapsed display string differs from the stored multi-line one, so :3347-3348 writes the collapsed copy. Stored text is multi-line in both save paths (rendered `content.textContent` — markdown.js:242 joins blocks with '\n', consumed at ui.js:2682 — and raw markdown `msg.text` at ui.js:3530-3546). Because ui.js:3289 is a shallow `slice().reverse()` over the memoized array from getBookmarks() (ui.js:2514-2519), `item` aliases `bmarks[realIdx]`, so the in-memory entry mutates too. All downstream matching is exact-string (isBookmarked ui.js:2526-2528, syncBookmarkButtonsForText :2541-2549, toggleBookmark :2568-2582), so on the next render of the chat the message's bookmark icon shows unsaved and clicking it pushes a duplicate entry that displays identically, making Remove look broken. Trim two claims: the Escape path (ui.js:3359-3365) does not necessarily blur immediately — it discards typed edits but still cannot prevent the collapse, since it resets textContent to the collapsed string before any later blur; and the icon revert needs a re-render (history/page reload) rather than being instant, and is partly pre-existing because the icon's initial state uses raw markdown `text` (ui.js:2669) while the save uses rendered `content.textContent` (ui.js:2682). Shipped bundle amd/build/ui.min.js contains the identical logic and is not stale.

### F41 - unrated

Two independent defects on the user-deleted cascade, one of which the claim misattributes. (1) Dead hook registration (primary, upstream): db/hooks.php:36 registers the callback for `\core\hook\user\deleted`, a class that does not exist in Moodle core. Core's user-deletion hook is `\core_user\hook\before_user_deleted` (~/Sites/moodle/user/classes/hook/before_user_deleted.php; dispatched per ~/Sites/moodle/lib/db/hooks.php:73), and lib/classes/hook/ has no `user/` subdirectory at all. db/events.php observes only `\core\event\course_deleted`, so no fallback exists. Consequence: `hook_callbacks::on_user_deleted()` (classes/hook_callbacks.php:191-239) is never invoked. Hard-deleting a Moodle user runs none of the cascade — not `conversation_manager::delete_user_data()` (:198), not the `local_ai_course_assistant_email_optout` purge (:206), not the Privacy-API leg (:214-225), and not the audit row (:226-231). This is broader residue than the claim asserts, and contradicts templates/privacy_notice.mustache:92. (2) Latent broken static call (secondary, only reachable if #1 is fixed): classes/hook_callbacks.php:214-217 calls `\core_privacy\manager::get_contexts_for_userid($userid, 'local_ai_course_assistant')` statically with two arguments, but privacy/classes/manager.php:219 declares it as a non-static single-argument instance method (`public function get_contexts_for_userid(int $userid): contextlist_collection`). PHP 8.3 throws `Error: Non-static method ... cannot be called statically` (verified by direct repro). The `catch (\Throwable)` at :232 swallows it into `debugging()`, so once the hook registration is repaired, `delete_user_data()` and the email_optout purge would succeed but the Privacy-API cleanup and the `user_deleted_cascade` audit row would still be skipped silently. The same broken static call appears at admin_user_data.php:112-115 in the admin purge path, wrapped in its own swallowing `catch (\Throwable)` at admin_user_data.php:123. Fix requires both: repoint db/hooks.php:36 at `\core_user\hook\before_user_deleted` (adjusting the parameter type at classes/hook_callbacks.php:191 and the accessor for the user object at :193), and replace the static calls at classes/hook_callbacks.php:214 and admin_user_data.php:112 with `(new \core_privacy\manager())->get_contexts_for_userid($userid)`, filtering the returned `contextlist_collection` to this component rather than passing a second argument.

### F42 - unrated

The learner self-service delete buttons under-delete. settings_user.php:137 (action=delete_all) and :118 (action=delete_course) call only conversation_manager::delete_user_data / delete_course_data, and those functions (classes/conversation_manager.php:422-438 and :487-503) omit five tables that hold per-user data and are declared as such in the plugin's own privacy metadata: obj_att (mastery attempts), flashcards (question/answer text), avatar_sess, email_optout, and sbx_rec (Soapbox transcripts plus storage_key/deck_key object-storage references; sbx_rec is assignid-keyed so the per-course variant cannot reach it at all). Neither learner action runs the Privacy API path that would cover them, so rows survive despite templates/privacy_notice.mustache:98 ("Deletion is immediate") and lang/en:857 ("permanently delete all your AI tutor conversations"). The docstring lede at conversation_manager.php:387-390 asserts the opposite ("sweeps every table in the plugin that stores per-user state"), and tests/conversation_manager_test.php:144-232 pins only six tables, so nothing catches the gap. Scope correction vs. the original claim: the admin purge (admin_user_data.php:110-125) and the core user_deleted hook (classes/hook_callbacks.php:198-221) both chase delete_user_data with provider::delete_data_for_user(), which does cover all five (obj_att/flashcards/avatar_sess at provider.php:883-885, email_optout at :930, sbx_rec via purge_soapbox_recordings() at :922) — so this defect is confined to the two learner-facing buttons, not to a hard user delete.

### F43 - unrated

privacy\provider::delete_data_for_all_users_in_context (classes/privacy/provider.php:804-817) is materially weaker than both the per-user erasure path in the same class and the course_deleted observer. It delegates to conversation_manager::delete_course_data() (15 tables, conversation_manager.php:487-503) and then redundantly re-deletes seven tables that list already covers (provider.php:810-816). Five tables holding learner content are never touched: obj_att (mastery attempts, install.xml:520), flashcards (Q/A text, :545), review_res (:568), avatar_sess (:619) and sbx_rec (Soapbox transcripts plus storage keys, :728). All five are deleted correctly when the request is scoped to one user (provider.php:883-885, :908-913, :922 -> purge_soapbox_recordings at :478) and by observer.php COURSE_TABLES/CHILD_TABLES (:48, 56, 61-62, 83). All five are also declared in get_metadata (:183, 188, 193, 203) and exported (:751-757), so the plugin advertises and exports data this erasure path leaves behind. The method is reachable independently of the observer: core \core_privacy\manager calls it for tool_dataprivacy context-expiry purges and context-scoped delete requests, where no course_deleted event fires. Course ids are reused, which is the orphan argument observer.php:69-73 already makes. Correction to the original evidence: the message_attachments file area is NOT covered by the per-user path either — the plugin's only delete_area_files call is attachment_manager.php:239 (per-message) and no erasure path purges the area, so that is a separate, broader defect and should not be cited as an asymmetry between these two methods.

### F46 - unrated

Learner-uploaded chat attachments (Moodle File API, component local_ai_course_assistant / filearea message_attachments, course context, itemid = msgs.id — attachment_manager.php:40-41, :229-253) are never exported by the privacy provider and are never deleted by any plugin-owned erasure path. classes/privacy/provider.php declares no add_subsystem_link('core_files') (metadata is 23 add_database_table + 4 add_external_location_link only), calls no writer::export_area_files (message export at :511-531 emits role/message/timecreated), and calls no delete_area_files in delete_data_for_all_users_in_context (:804-817), delete_data_for_user (:824+) or delete_data_for_users (:941+). Core does not cover this: files/classes/privacy/provider.php:38-39 states every component must export and delete its own files. The same gap applies to conversation_manager::clear_conversation (:372-382), delete_user_data (:407-460) and delete_course_data (:466+) — i.e. the learner self-service delete buttons (settings_user.php:118, :137) and the admin GDPR purge (admin_user_data.php:110) — to the user_deleted cascade (hook_callbacks.php:191-239; core's delete_user only clears the USER context at moodlelib.php:3746, while attachments sit in the COURSE context), and to the retention sweeper (task/conversation_retention.php:47-71). Attachments are on by default (settings.php:1047-1052 default 1; attachment_manager.php:65-69 treats unset as enabled) and the whole path is live (sse.php:372-393, chat.js:5043-5110). lib.php:98-105 makes the residue unreachable from a browser once the msgs row is gone, but stored_file rows and moodledata blobs persist and core_files is never disclosed in the privacy metadata. CORRECTION to the original claim: course deletion is NOT affected. Moodle core's remove_course_contents() calls $coursecontext->delete_content() (moodlelib.php:5014), which runs an unqualified $fs->delete_area_files($this->_id) (lib/classes/context.php:608-610) and removes every component's files in that course context, message_attachments included — so classes/observer.php:103-153 omitting file cleanup is not a defect. The only residual course-level edge case is a delete_course/remove_course_contents call passing keep_roles_and_enrolments or keep_groups_and_groupings, where delete_content() is skipped.

### F47 - unrated

Narrowly true but misdiagnosed and over-scoped. The three cited literals do exist and do ship (ui.js:4601, :4606-4607, :4615, reachable via chat.js:2558 -> ui.js:3912, appended at :4622), so on a white-labeled or non-English install the drawer's privacy teaser reads "My SOLA data" in English. But this is not a white-label bypass on a privileged privacy surface. It is three of ~32 hardcoded English literals in the same JS-built settings panel (ui.js:3972, :3974, :4241, :4276, :4566, plus :4755, :4829, :5579, :5695, :5766, :6123 elsewhere) — the panel has never been inside the branding-token system. The real and larger defect is that the runtime i18n mechanism intended to cover exactly this, chat.js `applyI18n`, is dead: it resolves `[data-i18n-key]` elements and substitutes `{$a}` with the branded `root.dataset.shortname`, but `data-i18n-key` is never emitted by any template or JS file (it matches only in chat.js, the consumer), so both of its querySelectorAll loops select nothing. Severity is cosmetic, not a data-protection impairment: the destination page settings_user.php is fully internationalized (get_string at :49, :50, :119, :125, :143, :164) and brand-aware (branding::filename_slug() at :95), with usersettings:title translated in the non-English packs (lang/es:298, lang/fr:270), so the learner reaches a correctly branded, translated page and the Art.15/17 controls work. The inline-cssText half of the claim should be dropped: the v6.8.1/v6.9.4 review item covered CSS in server-rendered markup (.drafts/v6.9.4-directory-reviewer-note.md:27; .wiki/Changelog.md:1327), ui.js uses cssText in 82 places, and run_validators.php enforces no such rule. Fix is cheap — route the three strings through get_string plus root.dataset.shortname, already used at ui.js:2929/:3459/:3850 — but the ticket worth filing is the never-emitted data-i18n-key attribute.

## Learning Radar (9)

### F38 - unrated

Both SOLA erasure cascades call the Moodle Privacy API through a statically-invoked instance method, so the provider half never runs and the failure is silently swallowed. admin_user_data.php:112-115 and classes/hook_callbacks.php:214-217 both call \core_privacy\manager::get_contexts_for_userid($userid, 'local_ai_course_assistant') statically; in Moodle core that is an instance method taking one int (privacy/classes/manager.php:219), the class has no __callStatic, and PHP 8 therefore throws Error "Non-static method cannot be called statically" (reproduced on PHP 8.3) before the extra argument is even considered. A second latent break sits one line later: contextlist_collection has no get_contextids(), so admin_user_data.php:120 / hook_callbacks.php:222 would throw even after fixing the static call. Both blocks are wrapped in catch (\Throwable) that only emits debugging(..., DEBUG_DEVELOPER) (admin_user_data.php:124-126, hook_callbacks.php:232-238), so the admin purge still redirects with NOTIFY_SUCCESS (~:132-138) and writes an admin_purge_learner_data audit row claiming completion. Only conversation_manager::delete_user_data() actually runs (classes/conversation_manager.php:407-453), whose hard-coded table map (:422-438) omits obj_att (mastery attempts), flashcards (Q/A text), avatar_sess, review_res, radar_sched, Soapbox recordings plus their object-storage media, and email_optout — all of which are purged only by provider::delete_data_for_user (classes/privacy/provider.php:880-891, :904-918, :919-922, :929-932). Net effect: an Article 17 purge from the admin page leaves mastery attempts, flashcard text, avatar sessions, review/radar rows, Soapbox media and the learner's email opt-out row (which contains their email) in place, with no operator-visible signal unless DEVELOPER debug display is on. The same gap (minus email_optout, which hook_callbacks.php:206-210 purges directly) applies to every core Moodle user deletion.

### F44 - unrated

Both plugin-owned Article 15 download bundles — the learner self-service one (settings_user.php:64-76, 11 sections) and the admin one (admin_user_data.php:54-65 plus the messages join at :84-88, 11 sections) — omit 10 learner-keyed tables: learner_memory, learner_goals, streak, struggle_signal, outreach_log, obj_att, flashcards, avatar_sess, email_optout and sbx_rec. The admin row-count preview (admin_user_data.php:186-194) iterates the same map, so it under-reports the total rather than listing those tables at zero. Most consequential is learner_memory.notes_json (db/install.xml:230-245), the model-inferred free-text observations that classes/privacy/provider.php:735-745 itself singles out as "precisely what a subject-access request exists to surface" — the v7.0.5 fix for that (the $latecoverage loop at provider.php:747-757) lives inside export_user_data and covers only the Moodle Privacy API path, leaving both buttons unchanged. The learner-facing copy over-claims: lang/en/local_ai_course_assistant.php:863 promises "a complete JSON copy of every {$a} record tied to your account". Two corrections to the original claim: review_res and radar_sched are not part of this (install.xml:568-586 and :588-617 give them no userid column — they key on staff resolved_by/creator, and the provider's own loop skips any table lacking userid), and the export/erasure asymmetry applies to only five of the ten — conversation_manager::delete_user_data (classes/conversation_manager.php:422-438) does purge learner_memory, learner_goals, streak, struggle_signal and outreach_log, while obj_att, flashcards, avatar_sess, email_optout and sbx_rec are missing from erasure too, which is a distinct Article 17 gap.

### F45 - unrated

The learner-facing privacy notice (templates/privacy_notice.mustache, rendered by privacy.php:55 and linked from the consent banner at hook_callbacks.php:880) overstates both the erasure and access rights, and hardcodes a configurable retention figure. (a) Line 92 claims all SOLA data tied to a user id is deleted when the Moodle account is deleted. It is not. hook_callbacks.php:197 runs conversation_manager::delete_user_data(), which globally empties the eight tables (conversation_manager.php:420-437) that privacy/provider.php:290-377 uses to build the contextlist read at hook_callbacks.php:214. The count()>0 guard at hook_callbacks.php:218 then skips privacy\provider::delete_data_for_user for every user with no Soapbox recording, leaving behind the rows only the provider purges: obj_att, flashcards, avatar_sess (provider.php:883-891), review_res.resolved_by (provider.php:905) and radar_sched.creator (provider.php:913). flashcard_manager::delete_user_data (flashcard_manager.php:203-205) would cover flashcards but is dead code with no callers. (b) Lines 96-97 promise "a complete copy of all data" from the user settings page, but the bundle is a fixed 11-table list at settings_user.php:64-76 and omits learner_goals, learner_memory, streak, struggle_signal, outreach_log, flashcards, obj_att, avatar_sess, sbx_rec and email_optout — several of which the formal privacy-API export does include (provider.php:749-753). (c) Line 86 states audit logs are kept "up to 365 days", but audit_retention_days is admin-editable (settings.php:1449-1455) and audit_cleanup.php:38-48 honours whatever is set, including 0 = never purge. Fix is either code (call delete_data_for_user before the global purge, or add the five tables to conversation_manager::delete_user_data and to the download bundle) or copy (soften lines 92/96-97 and make line 86 render the configured value).

### F59 - low

Scheduled Learning Radar reports posted to Slack/Teams keep the raw [SOLA_NEXT] block: classes/task/run_meta_ai_query.php:151,155 pass the unmodified provider response to radar_delivery::send_slack/send_teams, which never call format() and so never run strip_marker_tags (classes/radar_delivery.php:52 vs 223-247, 258-289), while classes/meta_ai_data_builder.php:88-101 mandates the block on every answer. The on-demand path is NOT affected — radar_export.php:124/136 receive the client-stripped lastResponse (amd/src/learning_radar.js:307-310, 274). Only [SOLA_NEXT] leaks, not [SOURCE:]/[OFF_TOPIC]/[NEEDS_ESCALATION], which the radar system prompt never requests.

### F60 - low

Slack metadata fields in radar_delivery::send_slack() are built with a single-quoted '\n' (classes/radar_delivery.php:237), so the literal characters backslash-n appear in the posted Slack message instead of a line break. Affects all three Slack callers — scheduled reports (classes/task/run_meta_ai_query.php:151), the anomaly digest (classes/task/run_anomaly_digest.php:132), and on-demand admin sends (radar_export.php:124) — each of which passes a non-empty $meta. Cosmetic only.

### F62 - low

radar_delivery::truncate_for_slack()/truncate_for_teams() truncate with byte-offset substr() (/Users/tom.caswell/ai-projects/ai_course_assistant/classes/radar_delivery.php:321-327, :335-341), so a report in a multi-byte script gets cut mid-UTF-8-sequence. json_encode() at :246 / :289 then returns false (JSON_ERROR_UTF8) with no flags and no false check; the file has no declare(strict_types=1) (namespace at :17), so false coerces to '' at the `string $body` parameter of http_post() (:300) and \curl::post() sends an empty body (:306). Delivery fails silently and the cron still records last_status='success' (classes/task/run_meta_ai_query.php:70). Real defect, but only reachable when an optional Slack/Teams webhook is configured AND the report exceeds 2800 bytes (18000 for Teams) AND the report text is non-Latin — so it does not affect the English dev fleet.

### F64 - low

Schedule modal never round-trips `filterprovider`: the field has no input, is never POSTed, and `radar_schedule_manager::save()` writes `''` over it on every edit — so the provider filter on a schedule migrated from the legacy `metaai_cron_filterprovider` config (db/upgrade.php:784) is silently dropped the first time an admin edits that schedule. Separately (UX nit, not silent): "Schedule…" prefills only query + name, not the course-ID/range scope of the query just run.

### F70 - low

Privacy erasure never deletes local_ai_course_assistant_radar_sched rows: both delete blocks condition on a `courseid` column the table does not have (it has `creator` + comma-separated `courseids`), so the DML call throws and the exception is swallowed. radar_sched and review_res are also declared in privacy metadata but skipped by the export loop's `userid`-column guard. Scope is limited to site administrators' own self-authored schedule config, not learner data.

### F89 - low

Date-range filtering is fully schema-backed and index-served on all three record kinds (msgs, _obj_att, _practice_scores). For summaries, _profiles rows are overwritten in place, so timemodified — not timecreated — is the date the visible text was produced; the codebase already relies on this (needs_update uses timemodified as its watermark). Separately, meta_ai_data_builder::build_student_profiles cannot be reused for a date-filtered view: no time parameter and a hard 50-row cap. Adjacent, not claimed: that query selects p.userid first, so get_records_sql keys by userid and collapses a learner's rows across courses when courseids is empty — a new viewer must select p.id first.

## Emergency controls (6)

### F02 - unrated

CONFIRMED, with one correction to the recovery claim. The emergency panel's Voice card derives its state from `voice_active_realtime`/`voice_active_realtime_backup` (emergency_admin.php:100-106) and never reads `emergency_voice_disabled`, which is the flag `emergency_control::disable()` actually sets (classes/emergency_control.php:84) and the only one `voice_registry::resolve()` enforces (classes/voice_registry.php:145-152). Because `voice_active_realtime` defaults to '' (settings.php:1641-1647), the backup is never written (classes/emergency_control.php:70-74) and the state expression evaluates false — so after a Voice disable on a default-configured site the card shows a green "Active" badge and offers only Disable again (templates/emergency_admin.mustache:59-64, 68-88, 90-99), while Realtime and hosted TTS return null site-wide. Correction to the reporter's impact statement: recovery does not strictly require shell or DB access — Disabling then Restoring the "all" card clears the flag, since restore()'s voice branch fires on FLAG_ALL (classes/emergency_control.php:139). That workaround is undocumented, requires master-killing the plugin first, and force-sets rag_enabled, rag_auto_reindex_drifted, and outreach_master_enabled back to 1 (classes/emergency_control.php:148-156) regardless of prior intent. Also, self-hosted Whisper STT is unaffected by the kill switch (it returns at classes/voice_registry.php:94-102, before the check), so "all voice dead" applies to Realtime and hosted TTS only.

### F22 - unrated

Confirmed. Neither weekly digest task consults the plugin master `enabled` flag nor `outreach_master_enabled`, so neither the MASTER KILL nor the Outreach emergency switch stops digest email, contradicting the panel copy. - classes/task/learner_weekly_digest.php:45-57 — execute() goes straight to the `digest_optin_%` user_preferences query; no kill-switch read anywhere in the file; email_to_user() at :159. - classes/task/instructor_weekly_digest.php:44-64 — same; enumerates `digest_email_enabled_course_*` only; email_to_user() at :128. - No upstream guard rescues either: objective_manager::is_enabled_for_course (classes/objective_manager.php:79-83) -> feature_flags::resolve (classes/feature_flags.php:49-58) reads only the mastery keys, and next_best_action::recommend (classes/next_best_action.php:63-65) does the same. - classes/emergency_control.php:64-67 / :92-95 write only `enabled` and `outreach_master_enabled`; disable() never touches \core\task\manager, and emergency_admin.php:81/:88 only wrap those calls — so "scheduled tasks stop" is unimplemented. - `outreach_master_enabled` has one functional consumer, classes/outreach_sender.php:79, and channel_admin_enabled (:162-171) returns false outside CH_STREAK7/CH_STREAK30/CH_COMPLETION (:50-52) — milestones only. Neither digest routes through outreach_sender. - Copy contradicted: lang/en/local_ai_course_assistant.php:625, :627; emergency_admin.php:32. Gated siblings for contrast: classes/task/send_reminders.php:35-37, classes/task/send_inactivity_reminders.php:39-41. Schedule live at db/tasks.php:103-125 (no `disabled` key). Two scope refinements to carry: 1. Blast radius is conditional on opt-ins — the learner digest needs `digest_optin_<courseid>=1` AND mastery enabled for that course (:96), the instructor digest needs `digest_email_enabled_course_<id>=1` (:51-57). On a site with no opt-ins both tasks return early and the bug is latent; a staging repro therefore requires setting one of those keys first. 2. The lang copy at :625 is wrong in a second way the claim understates: the Outreach switch also does NOT stop reminder emails. Study reminders and inactivity nudges are gated on `enabled` (+ `inactivity_reminder_enabled`), never on `outreach_master_enabled` — so of the three email families that string names, Outreach actually stops only milestones.

### F26 - unrated

emergency_control::restore() hard-writes outreach_master_enabled='1' for both FLAG_OUTREACH and FLAG_ALL (classes/emergency_control.php:153-156) with no saved baseline, because disable() at :92-95 writes '0' without stashing the prior value — unlike voice (:70-73 / :139-147) and chat (:113 / :163). Since the setting ships off (settings.php:2830-2835, default '0'), a MASTER KILL followed by Restore on emergency_admin.php (:88, card state keyed only on 'enabled' at :101) leaves the outreach master switch ON for a site that deliberately never enabled it. Impact is config-state drift, not mail: the restore notice does enumerate the touched keys (emergency_admin.php:88-94 → lang/en:615), and no email can fire because outreach_sender::send() still requires the per-channel admin switch (:83), the per-learner consent preference (:89, :186-190 — never written by the plugin), and the cooldown (:94-100). The rag_auto_reindex_drifted half of the original claim does not stand: '1' is that setting's shipped default (settings.php:708-712), and the task only reindexes drift-detected cmids (classes/task/auto_reindex_rag_drifted.php:53-71, early return at :54-57), not all 135 courses — so it is not the embedding spend the test constraints forbid. Fix: stash outreach_master_enabled on disable and restore from the stash, as voice does. Tester note: record outreach_master_enabled before touching emergency_admin.php and reset it by hand afterwards.

### F35 - unrated

CONFIRMED. The emergency panel's Voice row derives its state from the backup-config heuristic instead of the flag that actually enforces the kill, so on any site where `voice_active_realtime` is blank the card is wrong and unrecoverable from that page. Mechanism, all verified: - emergency_admin.php:103-104 sets the Voice row's `disabled` to `voice_active_realtime === '' && voice_active_realtime_backup !== ''`; `emergency_voice_disabled` is never consulted in that file. Compare the Chat row at :102, which reads `emergency_chat_disabled` directly. - classes/emergency_control.php:71-74 writes `voice_active_realtime_backup` only when the live label was already non-empty, while :84 sets `emergency_voice_disabled=1` unconditionally. Blank live label ⇒ flag set, no backup ⇒ predicate false ⇒ green "Active" badge after a successful Disable. - classes/voice_registry.php:145-151 is where the flag actually stops voice: `resolve()` returns null there, ahead of the `$rows[0]` fallback at :154-166 and the legacy-key fallback at :168-187. Per the comment at :132-144, a blank label alone is NOT a kill (it selects row 0), so voice can be live pre-switch and dead post-switch with the card reading Active. - templates/emergency_admin.mustache:90-99 renders the Restore form only inside `{{#disabled}}`, so the false state also removes the recovery control; the `{{^disabled}}` Disable form at :68-88 remains and merely re-writes the flag with no state change. - `emergency_voice_disabled` is not an admin setting (absent from settings.php; only the quicklink at settings.php:69-87 mentions "emergency"), and grep finds it nowhere else in PHP/JS/Mustache. The only clear is emergency_control.php:145, reachable in a browser only via the missing Voice Restore button or via MASTER KILL + Restore, which additionally forces enabled=1, rag_enabled=1, rag_auto_reindex_drifted=1, outreach_master_enabled=1 (:135-156) plus a site-wide widget outage. CLI `--restore --voice` is the only clean fix and needs SSH. Two refinements to the original claim. First, the severity has a latent form even on a site with no voice configuration at all: the flag persists in config with no UI representation, so voice stays dead the moment an admin later configures `voice_providers`, with the panel still reading Active. Second, the failure is specifically a read-out defect — enforcement (voice_registry.php:145) and disable/restore (emergency_control.php:84/:145) are both correct; only emergency_admin.php's state array was left on the pre-v7.0.5 heuristic. One-line fix: OR `emergency_voice_disabled` into emergency_admin.php:103-104 (ideally read the flag alone, mirroring the Chat row). Staging test, zero provider cost: on /local/ai_course_assistant/emergency_admin.php first confirm the live label is blank (Site admin > plugin settings, voice provider rows / `voice_active_realtime`), then tick Confirm on the Voice card and press Disable. PASS would be a red "Disabled" badge plus a Restore button; the code guarantees FAIL — green "Active" badge and the Disable form again. /local/ai_course_assistant/audit_log.php will show the `emergency_disable` row proving the action landed. Note the side effect: the flag is now set and, per the above, cannot be cleared from that page — plan to clear it with `php local/ai_course_assistant/admin/cli/emergency_disable.php --restore --voice`, or accept the MASTER KILL + Restore collateral (which also resets the outreach/RAG switches). Each POST also calls purge_all_caches() (emergency_admin.php:96).

### F36 - unrated

CONFIRMED. The "back to settings" button on both emergency_admin.php (line 132, rendered at templates/emergency_admin.mustache:45) and audit_log.php (line 142, rendered at templates/audit_log.mustache:51) links to /admin/settings.php?section=local_ai_course_assistant. That section name belongs to the plugin's admin_category (settings.php:48), not to its admin_settingpage, which is named local_ai_course_assistant_general (settings.php:54, added to the category at settings.php:2986). Moodle's admin/settings.php only serves admin_settingpage nodes: it calls $adminroot->locate($section, true) (admin/settings.php:20), and admin_category::locate returns the category itself (lib/adminlib.php:838-850), which fails the instanceof admin_settingpage check and throws moodle_exception('sectionerror') (admin/settings.php:22-25). No upstream validation or core fallback rescues it — core's local plugininfo load_settings() (lib/classes/plugininfo/local.php) does not pre-create a settingpage named local_<plugin>, and PARAM_SAFEDIR passes the name through unchanged. Result: clicking "Settings" from the two pages an operator opens mid-incident lands on Moodle's "Incorrect section" error page. The plugin already has the correct patterns in-tree — rag_admin.php:417 uses section=local_ai_course_assistant_general and rate_card_refresh.php:39 uses /admin/category.php?category=local_ai_course_assistant — so the fix is a one-token change in each of the two files. Verifiable on staging at zero cost via two read-only page loads.

### F39 - unrated

emergency_control::restore() is not the inverse of disable() for RAG and outreach. disable() stashes-then-clears only voice (classes/emergency_control.php:71-75, :84) and spend_cap_site (:108-112); for RAG/outreach it writes bare zeros (:87-95). restore() then hard-writes rag_enabled=1, rag_auto_reindex_drifted=1 (:149-150) and outreach_master_enabled=1 (:154) with no backup consulted — no such backup key exists in the class at all — and the FLAG_ALL branches at :148/:153 run those same lines, so a master-kill restore touches them regardless of pre-incident state. Nothing records which flags were actually disabled (flag_set(), :179-189), and both callers reach restore() unconditionally (emergency_admin.php:89; admin/cli/emergency_disable.php --restore, which defaults to FLAG_ALL). The provable consequence is the outreach master switch: it ships default '0' (settings.php:2834) and is documented as off on a fresh install with no outreach email firing while off, so kill-then-restore leaves a shipped-off safety switch on. Impact corrections vs the original claim: the RAG drift task cannot approach the forbidden 135-course reindex — it requires both rag_auto_reindex_drifted and rag_enabled (classes/task/auto_reindex_rag_drifted.php:44-51) and skips courses with no existing chunk rows (classes/rag_drift_detector.php:78-80), so a never-indexed site spends nothing; on this staging site RAG shows no divergence because both flags default to 1 (settings.php:701, :711), the same values restore writes; and outreach_master_enabled=1 alone does not send mail, since send() still needs the per-channel switch and learner consent (classes/outreach_sender.php:83, :88) and milestones_feature_enabled defaults to '0' (settings.php:2848).

## Avatar / talking avatar (6)

### F03 - unrated

Confirmed. For the two providers that use the local viewer page, the id embedded in the viewer URL and the id stored in the session row are different values, so the legitimate owner always fails the ownership check. talking_avatar_session_manager::start()'s 5th parameter is documented as the upstream provider session id (classes/talking_avatar_session_manager.php:56) and lands in upstream_session_id (:66), which is what user_owns_session() matches on (:149-153). The sole caller, talking_avatar_session.php:77-83, passes $session['session_token'] there. - HeyGen (unconditional): viewer sid = $newresp['data']['session_id'] from /v1/streaming.new (classes/talking_avatar/heygen_provider.php:84, 95-99), but the row stores $accesstoken from /v1/streaming.create_token (:61, :103). Different endpoints, so never equal; talking_avatar_viewer.php:60-71 throws moodle_exception('nopermissions') for the owner. The same mismatch makes talking_avatar_webhook.php:123 look the vendor's session_id up against a stored auth token, always missing and returning {"ok":true,"matched":false} (:124-127), silently discarding the authoritative duration/cost while returning 200 so the vendor will not retry. - D-ID (same shape): viewer sid = $resp['id'], row stores $resp['session_id'] (classes/talking_avatar/did_provider.php:67-68, 79-84, 88) — the code requires both non-empty separately and treats them as distinct, so the viewer check fails for the owner. Unlike HeyGen, D-ID's webhook path is unaffected, since the stored value is the vendor's session_id and talking_avatar_webhook.php:112-113 tries $payload['session_id'] first. Corroborating evidence this is a bug and not a convention: tavus_provider.php:90-91 and synthesia_provider.php:84-85 correctly place the real upstream id in session_token; only D-ID and HeyGen put an auth token in a column documented as an id. Impact: a learner clicking the avatar button creates both the DB row and the billable vendor session, sees a Moodle permissions error in the iframe, and the heartbeat (talking_avatar_session_end.php:47-63, driven by amd/src/ui.js's avatarSessionRowId set from json.session_rowid before the iframe loads) still closes and cost-stamps the row for a session nobody watched. tests/talking_avatar_ownership_test.php:36-48, 66-78 cannot catch it because it calls start() with a literal id and queries the same literal, bypassing talking_avatar_session.php's argument order. Requires the talking_avatar flag on plus a configured did/heygen key (hook_callbacks.php:559-563), so it is not reproducible on staging without paid vendor credentials.

### F08 - unrated

CONFIRMED. Once an admin has uploaded a file under Site administration → the plugin's "Custom avatars" filearea, that avatar appears as a tile in the learner gear/settings-panel Avatar grid but can never be selected. hook_callbacks.php:417-422 publishes its id as `custom:<contenthash>` (built at lib.php:152); the grid renders it unfiltered (ui.js:4133-4143, v7.2.7 numbering) and Save forwards the id (ui.js:4621-4623 → chat.js:2596-2602 → repository.js:190-195) to `local_ai_course_assistant_save_avatar_preference`, whose parameter is declared PARAM_ALPHANUMEXT (save_avatar_preference.php:41). Core's ALPHANUMEXT strips the colon and `validate_param` throws when the cleaned value differs (lib/classes/param.php:781, :618-621), so validate_parameters at save_avatar_preference.php:47 raises invalid_parameter_exception before the allowlist at :55-57 — which in any case lists only avatar_01..avatar_10 and avatar_12..avatar_17 — is reached. The rejection is invisible: core Ajax merely rejects the deferred (lib/amd/src/ajax.js:76-99, no notification, no console log) and chat.js:2601 is an empty `.catch`, so the tile stays highlighted, Save looks successful, and the avatar image never updates because chat.js:2597-2599 is never reached. The asymmetry that makes it look like a frontend bug is genuine: the same `custom:*` key resolves correctly as the site-wide default (hook_callbacks.php:396-406, settings.php:2434-2437). Corrections to the original report: the SVG avatar picker is not involved (localStorage only, no web-service call), and the avatar_11 gap plus the file-less avatar_12..avatar_17 entries are unreachable dead allowlist data, not a live defect, since only avatar_01..avatar_10 are ever offered by any UI. Fix direction: accept custom keys server-side (PARAM_RAW/PARAM_TEXT plus an explicit allowlist rebuilt from local_ai_course_assistant_get_custom_avatars(), or re-key uploads without the colon) and replace the empty `.catch` with a Notification.exception so any future rejection is visible.

### F09 - unrated

talking_avatar_session_manager::end() is documented as idempotent (classes/talking_avatar_session_manager.php:77) but is not: its only refusals are a non-webhook caller against a 'webhook' row (:94-96) and a 'sweeper' call against an already-closed row (:97-99), so a repeated 'heartbeat' — or a duplicate 'webhook' delivery — recomputes duration from started_at to now (:101) and overwrites ended_at/duration_sec/est_cost_usd (:106-110) with no MAX_OPEN_SECONDS clamp, since that clamp is sweeper-only (:102-105). talking_avatar_session_end.php checks ownership only (:58-61) with no already-closed check or rate limit, and the rowid is exposed to the client (amd/src/ui.js:6124), so a row's owner can re-close it repeatedly and inflate their own recorded minutes and est_cost_usd in the admin provider rollup. Correction to the original claim: the duplicate fire it cites is not reachable from the shipped UI — endAvatarSession early-returns on a zero rowid (ui.js:6040) and zeroes it after posting (ui.js:6061), and all four call sites (ui.js:1852-1853, :6079, :6093, :6140) go through it, so close-button + beforeunload is in fact safe (client-side, not server-side as the comment claims). The reachable trigger is a deliberately crafted or replayed owner POST touching only that owner's own row, making this a low-severity client-writable-cost-estimate defect rather than a routine double-count. Separately, the missing heartbeat clamp means even a single honest heartbeat after a long-idle tab records the full uncapped wall-clock span, where sweep_stale would have capped the same row at one hour.

### F14 - unrated

Narrowed and corrected: exactly two user-visible strings on the reachable avatar path bypass i18n — the 'Avatar' section heading (amd/src/ui.js:4151) and the panel 'Save' button (amd/src/ui.js:4636), both created as raw literals inside `showSettingsPanel` (ui.js:3918, invoked from amd/src/chat.js:2558). The runtime translator cannot reach them because `tagI18nElements` (chat.js:1416, run once at chat.js:1285) tags only static template DOM and `applyI18n` (chat.js:1564-1630) rewrites only `data-i18n-*`-tagged nodes, and amd/src/i18n_strings.js:35-75 has no keys for them. The other seven strings the original claim cites are in unreachable code: `showAvatarPicker` (ui.js:3678, 'Choose your avatar' 3696, 'Cancel' 3721) and `showSVGAvatarPicker` (ui.js:248, 'Presentation' 285, 'Skin' 315, 'Hair style' 343, 'Hair colour' 389, 'Save' 419) have no callers anywhere in amd/src — the header `.btn-avatar` (templates/chat_widget.mustache:207) has no click handler at all — so the British-spelling 'Hair colour' point is moot. The claim's reachable-path line numbers are ~21 lines stale. Finally this is not avatar-specific: the entire dynamically built settings panel is hardcoded English (e.g. 'My SOLA data', 'I'm a first-generation college student' at ui.js:4616; 78 literal `textContent` assignments in ui.js), so it is a panel-wide i18n coverage gap rather than an avatar-surface defect, and nothing here is a wrong, unreachable, or failure-swallowing code path.

### F15 - unrated

Two real, provable accessibility defects on the learner-facing widget, both present at v7.2.7. (1) The header avatar button is a fully focusable, labeled control with no behavior. templates/chat_widget.mustache:207-215 renders <button type="button"> with title and aria-label "Change avatar" and no disabled/tabindex="-1"/aria-hidden/inert; styles.css:3386-3387 applies cursor:default and pointer-events:none, which suppresses mouse and hover but leaves the button in the tab order and in the accessibility tree. No click or keydown handler for .local-ai-course-assistant__btn-avatar exists in amd/src or in the served amd/build bundles — the only reference, amd/src/chat.js:1462, merely re-localizes the label. The function that would service it, showSVGAvatarPicker (amd/src/ui.js:248), is exported at amd/src/ui.js:6200 but never called from anywhere, so the entire SVG-avatar customiser is unreachable code. A keyboard or screen-reader user tabs to a control announced "Change avatar", activates it, and gets nothing. That the omission is a defect rather than intent is shown by chat_widget.mustache:391, where the language banner is correctly neutralized with aria-hidden="true" inert, and by the dead :hover rule at styles.css:3399-3402 that pointer-events:none can never trigger. (2) The talking-avatar toggle never updates its pressed state. chat_widget.mustache:223 ships aria-pressed="false"; chat.js:1875-1880 wires the click to UI.toggleTalkingAvatar; ui.js:6072-6120 adds and removes the .aica-talking-avatar-panel without touching the attribute. Every aria-pressed writer in the codebase targets something else (ui.js:1857 mic, ui.js:3076 mode buttons, ui.js:3240 history views, chat.js:422 context-debug, chat.js:4074 quiz), so assistive tech is permanently told the panel is closed. Note the attribute is arguably the wrong one — a disclosure panel calls for aria-expanded — so the fix should correct the attribute, not just toggle it. Testability on staging: defect 1 renders unconditionally and reproduces in seconds (open a course, open the drawer, Tab to the first header control, press Enter — expect no panel and no state change), needing no student account. Defect 2 is gated at classes/hook_callbacks.php:560-563 on the talking_avatar feature flag plus a configured provider driver; with no such provider in staging's config the button will not render, so defect 2 is provable from code but likely not demonstrable in the browser without standing up a paid vendor integration.

### F71 - low

update_reminder_preferences.php:102 calls $DB->set_field('user','phone1',...) in the enable branch, but `global $DB;` appears only at :106 inside the else branch (:68 declares just $USER). `global` binds at runtime, so on the enable path $DB is null and the call is a fatal Error returned as a webservice exception — the WhatsApp reminder toggle cannot save. Reachable only when `reminders_whatsapp_enabled` is on (defaults to 0 in settings.php:1527-1532; reminder_manager::subscribe() at reminder_manager.php:61-63 throws first otherwise), which is why no test covers it. Secondary, milder: the intended write is authorised by `local/ai_course_assistant:use` in course context (:79-81), a student-held read capability, rather than `moodle/user:editownprofile` at context_user as used by every other own-profile write in the plugin (record_consent.php:56-58, dismiss_intro.php:51-53, save_avatar_preference.php:50-53). Self-write only, so no cross-user impact — the exposure is bypassing an auth-plugin phone1 field lock and writing an unnormalized PARAM_RAW string into a core column.

## Voice / STT / TTS (4)

### F01 - unrated

Real but narrower and lower-severity than claimed. For assistant messages of 220 characters or fewer (longer text routes to the chunked path, which clears correctly at chat.js:3722), a TTS fetch failure or non-JSON response lands in the terminal `.catch` at chat.js:3929-3931, or a synchronous throw lands in `catch (e)` at chat.js:3925-3927. Both call `Speech.speak(text, callback)` without `clearTtsLoadingState()`, so the speak button keeps the `--loading` pulse (styles.css:2776) while the learner hears the OS fallback voice. chat.js:3553 is the only remover of that class in the repo, and `UI.setSpeakingState` (ui.js:2207-2223) — reached via `onDone` at chat.js:3985-3989 — clears only `--active`, so nothing in the failure path resets it. Two corrections to the original claim: (1) the HTMLAudioElement fallback block at chat.js:3866-3924 also lacks any clear, but it is gated at chat.js:3777 on `sharedAudioCtx` lacking `decodeAudioData`, and `getOrCreateAudioCtx()` (chat.js:3502-3518) is called synchronously at chat.js:3968, so that block is unreachable on any browser with Web Audio — it is not a path a tester can hit; (2) the pulse is not permanent — `stopAllTts()` (chat.js:3569) clears it, and it is invoked both at the top of the next `handleSpeak` (chat.js:3948) and by the "Stop reading" button that is still on screen during the fallback (chat.js:3962-3973). So the defect is a stale visual state that survives until the learner's next speak interaction, on short messages only.

### F05 - unrated

The procedural SVG avatar is unreachable in the shipped UI, so the idle blink, the CSS mouth fallback and both Web-Audio mouth-sync loops have no DOM target. buildFaceSVG (amd/src/ui.js:151-169) is the sole producer of .aica-eye/.aica-mouth-* in the repo; it is reached only via renderAvatar → applyAvatarPrefs → initSVGAvatars (ui.js:229-241), which is gated on localStorage['aica_avatar'], or via showSVGAvatarPicker (ui.js:248), which has no caller — the header .btn-avatar has no click handler (chat.js:1462 sets only title/aria) and is pointer-events:none (styles.css:3382-3387). Stronger than the original claim: initUI unconditionally deletes the gating key immediately before reading it (ui.js:1125-1128, "Avatar picker has been removed; clear any stored face-SVG prefs"), and updateAvatarImages deletes it again on preset select (ui.js:3743), so even legacy saved prefs cannot re-enable the SVG; the shipped amd/build/ui.min.js contains the same removeItem-then-init sequence, so staging v7.2.7 matches. Dead as a result: styles.css:4146-4153 (blink), styles.css:4124-4131 (mouth fallback), ui.js:5118-5194 and ui.js:5199-5245 (mouth sync). Correction to the impact framing: avatar_animation_enabled (settings.php:2399-2404, per-course override hook_callbacks.php:722-728, policy_bundle.php:117) is NOT wholly unobservable — ui.js:1003 reads data-avataranim into avatarAnimEnabled and ui.js:2228-2236 gates the launcher-button 'aica-speaking' class on it, which drives the aica-speaking-glow keyframes at styles.css:4118-4122 with no SVG required. Turning the setting off therefore does visibly stop the TTS glow pulse, so the v6.5.0 A/B measures a glow rather than a face; the CLAUDE.md header-button list item 1 ("Avatar picker (.btn-avatar) — opens the avatar chooser") is stale documentation.

### F07 - unrated

Real but narrower than claimed. In the legacy voice tier's MediaRecorder/Whisper path, amd/src/voice.js:684-688 and the identical .catch at voice.js:691-695 collapse every transcribe.php failure — 429 rate limit, 400 missing file, 413 oversize, 415 bad MIME, 503 no STT provider, 502 SSRF, 502 upstream, 502 unparseable (transcribe.php:49-171) — into the same silent "restart listening" branch as a legitimately empty Whisper transcript. `data.error` is read for truthiness and its message thrown away; onErrorCb is never invoked, despite being held (voice.js:118-119) and used on this same path elsewhere (voice.js:824-827, 838-841) and despite chat.js:3189-3198 already implementing a proper onError that shows a message and ends the session. voice.js is the only caller of transcribe.php (voice.js:848, 680), so these failures have no other place to surface, and the tier can only be judged from the Network tab. Corrections to the reporter's framing: (a) it is not a timer-driven infinite loop — each retry needs a fresh detected utterance, since the VAD only stops the recorder after speech then silence (voice.js:600-613) and onstop skips the upload when !speechDetected (voice.js:659-663), with transcribe.php:49 capping at 20/60s; (b) most enumerated failures cost nothing, since 429/400/413/415/503/SSRF all exit before $curl->post (transcribe.php:157) and a dead Whisper URL exits at transcribe.php:161-165 unbilled — only the upstream-200-but-unparseable case at transcribe.php:167-171 spends money while hiding the error; (c) reachability requires both the legacy tier (chat.js:3252 skips it when realtimeenabled is set, per hook_callbacks.php:566/787) and a browser without SpeechRecognition (voice.js:845), so on a realtime_enabled site this code is not entered from the Voice tab at all. Severity is a UX/diagnosability defect, not a runaway-cost defect.

### F18 - unrated

CONFIRMED (soapbox.php:266-304). In server mode the terminal .catch at soapbox.php:304 is chained onto the .then() opened at line 266, so it reports any throw inside the getUserMedia fulfillment handler — the unguarded `new MediaRecorder(stream)` at line 272 (MediaRecorder undefined, or no supported default codec, since the guard at line 270 only covers the isTypeSupported feature test) and mediaRec.start() at line 302 — using the permission string at lang/en:1738, "Microphone access is needed to record...", pointing learner and tester at the wrong remedy. There is no upstream capability gate: soapbox_stt_mode (line 58, default 'server') flows through data-stt-mode (lines 66, 152) to the dispatch at line 346 with no browser probe or fallback, unlike the correct guard in browser mode at line 310. The captured stream also leaks on that path, since tracks are stopped only in mediaRec.onstop (line 275). Two refinements: if navigator.mediaDevices is undefined, line 266 throws synchronously in the click handler (line 345) before the promise exists, so the learner gets no alert at all rather than the wrong one; and severity is diagnostics/UX only, requiring a devtools override of window.MediaRecorder to reproduce on current Chrome.

## Soapbox (4)

### F11 - unrated

CONFIRMED. The chat-composer attachment upload has no server endpoint. amd/src/repository.js:41 builds `Config.wwwroot + '/local/ai_course_assistant/upload_attachment.php'` and fetch()es it as multipart POST at :42-46. That file does not exist in the plugin: absent from `git ls-tree v7.2.7` (the staging build), absent at HEAD (version.php:32 → 7.2.8), and `git log --all -- upload_attachment.php` is empty, so it has never existed. There is no substitute route either — db/services.php has no chat-attachment external function (its only `upload` entries, :358-374, are the unrelated Soapbox presigned-URL/register-recording/slide-render trio). The affordance is nevertheless live and reachable on staging: - settings.php:1058-1063 defaults `allow_student_attachments` to 1, and attachment_manager.php:65-69 returns true even when the setting was never saved. - hook_callbacks.php:872 passes `attachmentsenabled`; chat_widget.mustache:762-765 renders the hidden file input and :794-805 the paperclip button, gated on nothing else. - ui.js:2856-2857 resolves attachBtn/attachFileInput; chat.js:1729-1738 binds click → input.click() and change → handleAttachmentChange, which calls Repo.uploadAttachment at chat.js:5093. - The client gates at chat.js:5061-5083 do not block: staging's provider `gemini` is in attachment_manager.php:58 IMAGE_CAPABLE_PROVIDERS, and PDFs (settings.php:1067 default allowlist) skip the image gate entirely. The served bundles carry the dead URL and are not stale: `git show v7.2.7:amd/build/repository.min.js` contains upload_attachment.php; amd/build/chat.min.js contains `attachment:error_provider_no_images` and `aica-attachment-btn--uploading`; amd/build/ui.min.js contains `aica-attachment-btn` and `aica-attachment-file-input`; and `git log -1 --format=%ct` is identical for src and build on chat/ui/repository. Failure mode as described: the webserver returns an HTML 404, so `resp.json()` (repository.js:47) rejects with a JSON SyntaxError before the intended `data.error` handling at :48-49 can run, and chat.js:5103-5105 surfaces `err.message` — the user gets a toast containing a raw parse error rather than a useful message. Everything downstream is real code that is unreachable through the UI because no draftitemid can ever be produced: sse.php:120 (param), :372-392 (promotion + payload prep), :630-642 (PDF text injection), :705-708 (provider re-check), :729-732 (image content block), :795-796 (meta event), and lib.php:46 (pluginfile serving). Only correction to the original claim: the fetch spans repository.js:42-46 and `resp.json()` is at :47 (claim said :43-47 and :48). The substance is accurate.

### F17 - unrated

CONFIRMED. score_speech.php:186-206 passes an unwrapped JSON Schema as `response_schema`, but the receiving builder — openai_compatible_provider.php:155-165, the only response_format construction site in classes/provider/ and the one gemini_provider.php:26-34 inherits unmodified — reads `$schema['schema']` at line 161 with no fallback. Every Gemini speech-scoring request is therefore built with `"json_schema":{"name":"structured_output","schema":null,"strict":true}` (reproduced under PHP 8.3 with the literal array). Nothing normalizes the shape in between: base_provider.php:936-937 instantiates gemini_provider, create_from_config never touches $options, and chat_completion hands $options straight to build_body. The wrapper is the house convention (objective_manager.php:874-876, conversation_classifier.php:132, generate_quiz.php:480-483; claude_provider.php:233-238 expects it as well), and the same unwrapped mistake exists at score_essay.php:90-109 and generate_flashcards.php:112/:169-182. Structured output is not in force for speech scoring: the only JSON guarantee is the prose instruction at score_speech.php:170-178 plus the regex salvage at :212-217, and a provider rejection is flattened to empty_result('provider_error') at :208-210 (:348-357), discarding the real reason. It also affects the presentation-assignment path via soapbox_scorer.php:165. No test covers it — tests/score_speech_test.php exercises only the disabled/too_short/boundary/returns-structure/mode_hint branches. ONE CORRECTION to the original report: the `PHP Warning: Undefined array key "schema"` is not unconditional. Moodle sets error_reporting($CFG->debug) (lib/setup.php:604,747), so at DEBUG_NONE the warning is neither displayed nor logged and cannot break the AJAX JSON body; that risk exists only with developer debug plus display on. The malformed `schema:null` payload, by contrast, is transmitted on every scored speech at any debug level.

### F19 - unrated

CONFIRMED, with a broader trigger than claimed. The ESL/level preset criteria are a one-way fallback that ordinary activity silently and permanently destroys. score_speech.php:122/124 uses `$preset['criteria']` only when `rubric_manager::get_active_rubric($courseid, 'speech')` is falsy, and that returns null only when no active speech rubric exists at either the course or the courseid=0 scope (rubric_manager.php:116-132). Two paths seed an active courseid=0 speech rubric with the GENERAL `DEFAULT_SPEECH_CRITERIA` (rubric_manager.php:317-332 → :344-346 → :43-49), neither of which consults `soapbox_level_course_<ID>`: - rubric_admin.php:140 — `ensure_default_rubric($type)` on plain page load, after the POST block closes at :136, so a GET with no sesskey seeds it. The "Edit speech rubric →" button at course_settings.php:738-741 links directly here with `type=speech`. - get_rubric.php:63 — `ensure_default_rubrics()`, which despite its stale docblock (rubric_manager.php:207-208, "conversation and pronunciation") also seeds speech at rubric_manager.php:234-242. It runs BEFORE `$params['type']` is examined, so the speech row is created even when the caller asked for another type. chat.js:2718 (`startPracticeSession`) calls this for 'conversation'/'pronunciation', meaning any learner starting conversation or pronunciation practice on any course seeds the global general speech rubric. Nobody needs to visit the speech rubric tab. Correction to the original claim: the presets are not unreachable from install. db/upgrade.php:431-445 seeds only conversation and pronunciation, so before either trigger fires the level presets genuinely apply. The defect is irreversibility plus silence — after the first trigger, an "ESL (beginner)" course is scored against "Delivery & Fluency"/"Time Management" while only the coaching hint at score_speech.php:139 still reflects the level, and no UI anywhere signals the half-applied setting. Recoverable only if an admin knows to load the preset via rubric_admin.php:158-165 (`?preset=<level>`) and Save. Also in scope: because classes/soapbox_scorer.php routes through the same score_speech::execute, the presentation-assignment path inherits this. Testable on staging without a student account: set a course to "ESL (beginner)" (two saves, per the surface notes), query the rubrics table for `courseid=0, type='speech'` to confirm no row exists, run one Soapbox scoring call and check the criterion names in the resulting practice_scores row are the ESL set; then GET rubric_admin.php?type=speech (or trigger any conversation practice session), re-check that the courseid=0 row now exists, and re-score — the criterion names revert to the general set with the level setting unchanged.

### F92 - low

transcript_report.php's outcome filter joins through _obj_att.msgid (classes/transcript_report.php:157), and msgid is populated only by the conversation classifier (conversation_classifier.php:165-173 passes $usermsgid; record_objective_attempt.php:70-77 passes six args, score_speech.php:290-299 passes null). But the objective picker is populated from every objective in the course, unfiltered (transcript_report.php:113-114), so selecting a quiz- or rubric-assessed outcome always yields zero rows and reads as "nobody discussed this" rather than "no conversational evidence exists." Separately, db/install.xml:526 documents _obj_att.source as "quiz, conversation" while score_speech.php:295 writes a third literal, 'rubric'.

## Widget UI / i18n (4)

### F27 - unrated

On any course front page (/course/view.php), the "Generate flashcards from this page" starter chip is rendered but guaranteed to fail, and its failure prints a raw internal status code to the learner in hardcoded English. Chip visibility is gated solely on the per-course `flashcardsenabled` flag (templates/chat_widget.mustache:576-585; hook_callbacks.php:866), while the widget itself injects on both course and module contexts (hook_callbacks.php:255-258). But `$currentpageid` is only set inside a `CONTEXT_MODULE && $PAGE->cm !== null` branch (hook_callbacks.php:488-496), and the client-side recovery reads a cmid only from a `cmid-N` body class that course pages do not carry (amd/src/chat.js:1221-1229). So the handler sends `currentPageId || 0` = 0 (amd/src/chat.js:2203), and classes/external/generate_flashcards.php:62-67 short-circuits to empty content and returns `['success' => false, 'message' => 'no_page_content']` before reaching a provider. amd/src/chat.js:2212-2213 then interpolates that code into a hardcoded English sentence: "I could not generate flashcards from this page (no_page_content)." Both branches bypass i18n: the success message is hardcoded English too (amd/src/chat.js:2208-2209), and `flashcards:generated` (lang/en/...:1630) has zero callers anywhere in *.php, *.js or *.mustache. Scope corrections vs. the original report: (a) `flashcards:generated` is NOT a translated string awaiting use — it carries the identical English value in all 46 locales (verified by uniq -c), so adopting it enables future translation rather than fixing localization today, and its `{$a}` form drops the markdown review-page link the current success path emits; (b) the failure path returns before any provider call, so it incurs no API cost — this is a UX/i18n defect only; (c) the chip functions correctly on activity pages, so the guaranteed failure is limited to non-module contexts, most visibly the course landing page where the starters overlay opens by default. `flashcardsurl` is correctly populated (hook_callbacks.php:867-870) and is not part of the defect.

### F32 - unrated

Confirmed with corrections. All 16 `flashcards:` lang keys are present but byte-identical untranslated English in all 45 non-English locales (verified by parsing every lang file; neighbouring namespaces path_ 0/8, pathpanel_ 0/3, mastery: 0-2/17 identical, so the block was skipped rather than coincidentally overlapping). Twelve of the sixteen are learner-facing — flashcards.php:67-81 (Question/Answer/Reveal answer/Again/Hard/Easy, intro, empty state, session complete), flashcards.php:41 (`flashcards:disabled` exception), templates/chat_widget.mustache:583 (starter chip) — so the review page and widget chip render in English in every locale. Three (`flashcards:link`, `:toggle`, `:toggle_help`) are admin-only at course_settings.php:640,:649, where English fallback matches the deliberate precedent in ADMIN_DIAGNOSTIC_UNTRANSLATED; `flashcards:generated` is referenced by no code at all (dead string). The release gate cannot see any of this: tests/lang_completeness_test.php:638 tests only `array_key_exists`, never value inequality, so present-but-untranslated passes and CLAUDE.md's "46/46 with zero missing keys" is true yet uninformative. lang/en:1615 and :1626 duplicate `flashcards:disabled` with the same value (harmless, but evidence the block was appended unreviewed). `objectives:` is likewise 3-6/41 identical across locales. Classification: this is an i18n content gap plus a testing-gate weakness (parity check should compare values with an explicit allowlist), not a wrong/unreachable/failure-swallowing code path.

### F34 - unrated

CONFIRMED. In sse.php's live stream filter the 24-byte holdback splits the stream on a byte offset (`$emit = substr($buf, 0, -24); $carry = substr($buf, -24);`, sse.php:878-879), so a 2-4 byte UTF-8 character straddling that offset is cut in half. The malformed $emit makes json_encode return false at sse.php:296 (`echo 'data: ' . json_encode($data) . "\n\n";` — no flags, no return check), `'data: ' . false` yields the bare line "data: \n\n", and amd/src/sse_client.js:117-118 skips it because trim() leaves "data:" which fails startsWith('data: '). One to two SSE events are lost per split, silently, with nothing logged at any layer. Corrections/refinements to the original write-up: (a) the provider is NOT the splitter — openai_compatible_provider.php:228,244 hands the callback json_decode'd, character-aligned UTF-8 strings, so SOLA's own holdback is the sole cause and the cut offset (`strlen($buf) - 24`) is independent of provider chunk boundaries; with aligned chunks the bug still fires on 299/300 simulated responses (~6.7 dropped events each). (b) The identical failure also applies to the final-tail emit in $streamflush (sse.php:899-901). (c) The client-side conversation carryover is corrupted too, not just the rendered bubble — chat.js:5407 records parsed.text (built only from the surviving tokens) into localStorage — while the DB row from sse.php:869 keeps the correct text, so server-side history and audit logs disagree with what the learner saw. (d) Pure-ASCII responses are immune; the exposure is any response mixing ASCII with multibyte characters (em-dash, curly quote, accented letter, emoji, or any non-Latin script), i.e. routine across the plugin's 46 languages.

### F75 - info

amd/src/ui.js:3002 interpolates `displayName` (from `root.dataset.shortname`/`displayname`, i.e. the raw PARAM_RAW branding config) into the welcome panel's innerHTML without the `escHtml` helper that the same string applies to the title at :2975 and subtitle — a missed escape inside the block hardened for exactly this (comment at :2960-2964). The value is only writable with moodle/site:config (settings.php:2360-2371 admin_setting_configtext, no paramtype; no other writer exists — no external function, no per-course override, not on policy_bundle ALLOWED_KEYS), so there is no privilege escalation and this is escaping hygiene / defense-in-depth, not an exploitable stored XSS. Fix: wrap in escHtml, matching settings.php:35 which already htmlspecialchars the same value.

## Outreach & digests (3)

### F21 - unrated

Confirmed: in classes/external/update_reminder_preferences.php, execute() declares only `global $USER;` at line 68, but the WhatsApp branch calls `$DB->set_field('user','phone1',...)` at line 102 while the file's only `global $DB;` is at line 106 inside the mutually exclusive `else` branch. Because PHP binds `global` at runtime, $DB is null at line 102: PHP 8.3 raises "Undefined variable $DB" then fatals with "Call to a member function set_field() on null" (reproduced locally on php@8.3). reminder_manager::subscribe() has already committed the reminders row (classes/reminder_manager.php:96-107, no wrapping transaction), so the learner ends up subscribed to WhatsApp reminders while the web service returns an exception and amd/src/chat.js:2613-2616 discards it in an empty `.catch`, leaving the UI silent. Precision correction to the original report: the branch is gated by classes/reminder_manager.php:61-63, which throws when reminders_whatsapp_enabled is off (default 0, settings.php:1527-1532), so this is NOT reproducible on staging as configured — it is a latent fatal that fires on any site that turns the WhatsApp channel on and submits a phone number. Fix is to add $DB to the `global` declaration at line 68.

### F24 - unrated

Milestone outreach is permanently inert: outreach_sender::send() blocks at classes/outreach_sender.php:89 on learner_consents(), which requires user preference 'sola_outreach_milestones' === '1' (:186-190, :200-202), and no production code path writes that preference — no page, external function (db/services.php), template, JS, upgrade step, or Moodle-core-writable declaration (lib.php has no ..._user_preferences() callback). The docblock's "Communications settings panel" (:174-177) does not exist, and its companion reader get_log_for_learner() (:229-232) is likewise uncalled. The task is registered (db/tasks.php:193) and does call send() (classes/task/milestone_check.php:100, :138), so with outreach_master_enabled and milestones_feature_enabled both on, every attempt returns bare false at :90 with no log line and no audit row, making the broken state indistinguishable from off. Unit tests pass only because they set the preference directly. Caveat on framing: the admin description (lang/en empathy:milestones_enabled_desc) does disclose that per-learner consent is required, so the copy is not false — there is simply no mechanism for a learner to grant it. Related: learner_goals_manager::save() (classes/learner_goals_manager.php:65) also has no non-test caller, so the same v5.3.0 feature cluster is unwired on the goals side too.

### F30 - unrated

The "Email me study session notes" checkbox in the widget settings panel is a dead control. It renders only when email reminders are enabled for the course (amd/src/ui.js:4558, gated on config.emailRemindersEnabled from amd/src/chat.js:2569), is built at amd/src/ui.js:4557-4572, seeded from localStorage at :4564, and persisted to the browser-only key aica_email_notes at :4690-4695. Nothing ever reads that key except the line that re-checks the box, and because it never leaves the browser no server-side task can act on it. The client wrapper that would deliver on the promise, repository.js:359-364 emailStudyNotes (exported :567), has zero call sites in any *.js/*.php/*.mustache in the repo and is absent from the built chat.min.js and ui.min.js bundles, so the registered external function (db/services.php:179-185 → classes/external/email_study_notes.php, message_send at :98) is unreachable from the UI and no study-notes email is ever sent. The server side looks healthy in a db/services.php audit, which is why the gap survives review.

## Prompt & injection (3)

### F74 - low

Defense-in-depth gap: the LLM-generated learner profile is persisted with only trim() (classes/student_profile_manager.php:150-156, 176-186) and re-injected unfenced into the learner's own system prompt under a "Personalise your responses" directive (classes/context_builder.php:466-476), while the course-content paths on the same builder are fenced. Because the channel is seeded and consumed by the same learner, it grants no capability the learner does not already have from their own user turns; the only genuine delta is persistence across resets. Low, not medium.

### F76 - medium

Client-supplied `pagetitle` (sse.php:106, PARAM_TEXT, never re-derived or sanitized) is interpolated verbatim into two instruction-region spots in the system prompt — the priority-80 `topic_focus` section (context_builder.php:507, 1900) and the sentence that establishes the untrusted fence (context_builder.php:523, 534) — while every sibling untrusted input on the same path is fenced (chunks :305, RAG-off dump :346, page body :531) and the sibling `pageid` is re-validated (:930-937). Self-directed jailbreak-hardening gap only: sse.php is sesskey-gated, the value is echoed back solely to the same requester (sse.php:798), it reaches $DB only as a bound parameter, and the learner already controls an unfenced PARAM_RAW `$message` in the same request. The persistent leg via struggle_classifier → learner_memory_manager (sse.php:454-461, struggle_classifier.php:192-196, learner_memory_manager.php:226, 355-361) is real but gated behind `struggle_classifier_enabled` (default 0, settings.php:2861-2867) plus a stage-2 'frustrated' label.

**Status:** OPEN - verified 2026-09-03: sse.php:106 optional_param pagetitle PARAM_TEXT -> context_builder::get_topic_focus_instructions.

### F78 - low

The "## Course Structure" prompt section (section names, section summaries, activity names) is the one course-authored path into the system prompt that never calls security::fence_untrusted() or security::sanitize_rag_chunk(), unlike the current-page, RAG-chunk, and RAG-off dump paths. Real but low: the adversary needs course-editing capability, the payload is capped at 2500 chars (1500 with a page anchor), section names are HTML-escaped by format_string() and summaries are strip_tags'd + capped at 200 chars (only activity names are fully raw), and a site-wide CAT_SAFETY/100 section already tells the model never to follow instructions embedded in course pages.

## Attachments (1)

### F12 - unrated

Six attachment error keys (attachment:error_disabled, error_no_file, error_upload_failed, error_too_large, error_type, error_save_failed — lang/en/local_ai_course_assistant.php:44-49) are defined in all 46 lang files but referenced by no code at v7.2.7 or HEAD, matching the set a never-shipped upload endpoint would have emitted. Meanwhile the two client-side gates that do fire use literal English: amd/src/chat.js:5063 and :5070, in contrast to :5076 which uses Str.get_string. These gates are reachable — attachments default on (classes/attachment_manager.php:65-69, settings.php:1048-1053), the paperclip renders (classes/hook_callbacks.php:872, templates/chat_widget.mustache:794-805) and the change handler is bound (amd/src/chat.js:1730-1738) with both checks running before the (nonexistent) upload call at :5104. tests/lang_completeness_test.php:148/:625 check only key reference and key parity, never values, so this class of gap is invisible to the suite. CORRECTION to the original claim: the strings are NOT translated — all seven attachment:error_* values are untranslated English in every non-English locale (lang/es:1294-1300, lang/fr:1267-1273, lang/ja:1267-1273, lang/zh_cn:1290-1296, lang/ar:1267-1273, lang/sw:1314-1320), including the one string the code already localizes. So the fix is two lang lookups plus real translation for seven keys across 45 locales, and the severity is i18n hygiene rather than lost prepaid translation.

## Other (3)

### F63 - low

build_transcript() fetches the full matching message set with an unbounded get_records_sql() and only truncates afterwards in PHP; because the ORDER BY is courseid/userid ascending and the loop breaks on first overflow, truncation deterministically drops the highest-numbered courses, while the block header still reports the unfiltered fetch count as "Total messages in transcript".

### F72 - medium

course_settings.php renders the stored per-course provider API key in cleartext into the served HTML (line 410-411, `value="<?php echo s($current->apikey) ?>"` on a hand-rolled type="password" input). The page is gated only on the COURSE-context capability local/ai_course_assistant:manage (course_settings.php:52), which db/access.php:50-59 grants to editingteacher and manager archetypes, so a course-level actor can read a credential a site admin configured for that course. The key is stored plaintext (course_config_manager.php:60,:73; db/install.xml:159 text column) and no capability check wraps the render. Contrast apibaseurl, re-gated on moodle/site:config at course_settings.php:109-119 for exactly this threat model, and the site-wide key, which correctly uses admin_setting_configpasswordunmask in settings.php:277-282 and is never echoed on this page. Exposure is conditional on a per-course override having been saved, which bounds real-world impact on the current fleet (single site-wide key), so the issue is a latent privilege-boundary credential disclosure rather than an actively leaking secret.

**Status:** OPEN - verified 2026-09-03: course_settings.php:410-411 renders value="<?php echo s($current->apikey) ?>" into the served HTML.

### F86 - low

email_study_notes (WS endpoint, ajax=true, capability :use) has no rate limiter and no length clamp on its PARAM_RAW `notes`, so an authenticated learner can loop self-notifications through the site mail relay. Not a paid-provider/spend-cap gap — nothing in this path calls an AI provider — and the body is bounded by PHP's post_max_size, not literally unbounded. The endpoint is also unreachable from the shipped UI (its repository.js helper has no caller), so it is deliberate-abuse-only.
